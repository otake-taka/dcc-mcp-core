# This file is automatically generated by `just stubgen`.
# Do not edit manually — your changes will be overwritten.
# To regenerate: cargo run --bin stub_gen --features stub-gen

from __future__ import annotations

import builtins
import typing

# ── Module metadata ──

__version__: builtins.str = "0.20.9"  # x-release-please-version
__author__: builtins.str = "Hal Long <hal.long@outlook.com>"

# ── Constants (registered via m.add() in src/lib.rs::register_constants) ──

APP_NAME: builtins.str
APP_AUTHOR: builtins.str
DEFAULT_DCC: builtins.str
DEFAULT_VERSION: builtins.str
DEFAULT_MIME_TYPE: builtins.str
SKILL_METADATA_FILE: builtins.str
SKILL_SCRIPTS_DIR: builtins.str
SKILL_METADATA_DIR: builtins.str
ENV_SKILL_PATHS: builtins.str
ENV_LOG_LEVEL: builtins.str
DEFAULT_LOG_LEVEL: builtins.str
ENV_LOG_FILE: builtins.str
ENV_LOG_DIR: builtins.str
ENV_LOG_MAX_SIZE: builtins.str
ENV_LOG_MAX_FILES: builtins.str
ENV_LOG_ROTATION: builtins.str
ENV_LOG_FILE_PREFIX: builtins.str
DEFAULT_LOG_FILE_PREFIX: builtins.str
DEFAULT_LOG_ROTATION: builtins.str
DEFAULT_LOG_MAX_SIZE: builtins.int
DEFAULT_LOG_MAX_FILES: builtins.int


# This file is automatically generated by pyo3_stub_gen
# ruff: noqa: E501, F401, F403, F405

import builtins
import enum
import pathlib
import typing
__all__ = [
    "AuditEntry",
    "AuditLog",
    "AuditMiddleware",
    "BoundingBox",
    "CaptureBackendKind",
    "CaptureFrame",
    "CaptureResult",
    "CaptureTarget",
    "Capturer",
    "DccCapabilities",
    "DccError",
    "DccErrorCode",
    "DccInfo",
    "DccLinkFrame",
    "DispatchStatus",
    "EventBus",
    "FileRef",
    "FrameRange",
    "GracefulIpcChannelAdapter",
    "GuiExecutableHint",
    "InputValidator",
    "InstanceStatus",
    "IpcChannelAdapter",
    "LoggingMiddleware",
    "McpServerHandle",
    "ObjectTransform",
    "PromptArgument",
    "PromptDefinition",
    "PyBufferPool",
    "PyCrashRecoveryPolicy",
    "PyDccLauncher",
    "PyProcessMonitor",
    "PyProcessWatcher",
    "PyPumpedDispatcher",
    "PySceneDataKind",
    "PySharedBuffer",
    "PySharedSceneBuffer",
    "PyStandaloneDispatcher",
    "RateLimitMiddleware",
    "RecordingGuard",
    "RenderOutput",
    "ResourceAnnotations",
    "ResourceDefinition",
    "ResourceTemplateDefinition",
    "SandboxContext",
    "SandboxPolicy",
    "SceneInfo",
    "SceneNode",
    "SceneObject",
    "SceneStatistics",
    "ScriptLanguage",
    "ScriptResult",
    "SdfPath",
    "SemVer",
    "SerializeFormat",
    "ServiceEntry",
    "ServiceStatus",
    "SkillBranding",
    "SkillCatalog",
    "SkillGroup",
    "SkillLinks",
    "SkillMetadata",
    "SkillRuntimeDescriptor",
    "SkillRuntimeKind",
    "SkillRuntimeReport",
    "SkillRuntimeState",
    "SkillRuntimeSummary",
    "SkillScanner",
    "SkillScope",
    "SkillSummary",
    "SkillWatcher",
    "SocketServerAdapter",
    "TelemetryConfig",
    "TimingMiddleware",
    "ToolAnnotations",
    "ToolDeclaration",
    "ToolDefinition",
    "ToolDispatcher",
    "ToolMetrics",
    "ToolPipeline",
    "ToolRecorder",
    "ToolRegistry",
    "ToolResult",
    "ToolValidator",
    "TransportAddress",
    "TransportScheme",
    "UsdPrim",
    "UsdStage",
    "VersionConstraint",
    "VersionedRegistry",
    "VtValue",
    "WindowFinder",
    "WindowInfo",
    "artefact_get_bytes",
    "artefact_list",
    "artefact_put_bytes",
    "artefact_put_file",
    "atomic_write_bytes",
    "atomic_write_text",
    "bytes_digest_sha256",
    "correct_python_executable",
    "deserialize_result",
    "ensure_within_root",
    "error_result",
    "file_digest_sha256",
    "from_exception",
    "gc_orphans",
    "init_default_telemetry",
    "is_gui_executable",
    "is_telemetry_initialized",
    "lz4_compress",
    "lz4_decompress",
    "mpu_to_units",
    "normalize_tool_arguments",
    "normalize_tool_meta",
    "parse_skill_md",
    "scan_and_load",
    "scan_and_load_lenient",
    "scan_and_load_strict",
    "scan_and_load_team",
    "scan_and_load_team_lenient",
    "scan_and_load_user",
    "scan_and_load_user_lenient",
    "scan_skill_paths",
    "scene_info_json_to_stage",
    "serialize_result",
    "shutdown_telemetry",
    "stage_to_scene_info_json",
    "success_result",
    "units_to_mpu",
    "validate_action_id",
    "validate_action_result",
    "validate_tool_name",
]

@typing.final
class AuditEntry:
    r"""
    Python representation of a single sandbox audit entry.
    """
    @property
    def timestamp_ms(self) -> builtins.int:
        r"""
        Unix timestamp in milliseconds when the action was recorded.
        """
    @property
    def actor(self) -> typing.Optional[builtins.str]:
        r"""
        Actor / caller identity, or `None`.
        """
    @property
    def action(self) -> builtins.str:
        r"""
        Name of the action that was invoked.
        """
    @property
    def params_json(self) -> builtins.str:
        r"""
        Parameters as a JSON string.
        """
    @property
    def duration_ms(self) -> builtins.int:
        r"""
        Duration in milliseconds.
        """
    @property
    def outcome(self) -> builtins.str:
        r"""
        Outcome as a string: ``"success"``, ``"denied"``, ``"error"``, or ``"timeout"``.
        """
    @property
    def outcome_detail(self) -> typing.Optional[builtins.str]:
        r"""
        Outcome detail string (denial reason or error message), or ``None``.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class AuditLog:
    r"""
    Read-only Python view of the sandbox audit log.
    """
    def __len__(self) -> builtins.int:
        r"""
        Total number of entries.
        """
    def entries(self) -> builtins.list[AuditEntry]:
        r"""
        Return all entries as a list of :class:`AuditEntry`.
        """
    def successes(self) -> builtins.list[AuditEntry]:
        r"""
        Return only successful entries.
        """
    def denials(self) -> builtins.list[AuditEntry]:
        r"""
        Return only denied entries.
        """
    def entries_for_action(self, action: builtins.str) -> builtins.list[AuditEntry]:
        r"""
        Return entries for a specific action name.
        """
    def to_json(self) -> builtins.str:
        r"""
        Return all entries serialised as a JSON string.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class AuditMiddleware:
    r"""
    Audit middleware — accumulates an in-memory log of all dispatched actions.

    ```python
    audit = AuditMiddleware()
    pipeline.add_audit()
    pipeline.dispatch("my_action", "{}")
    for record in audit.records():
        print(record["action"], record["success"])
    ```
    """
    def __new__(cls, record_params: builtins.bool = True) -> AuditMiddleware:
        r"""
        Create a new audit middleware.

        Args:
            record_params: If True, include parameters in each audit record (default: True).
        """
    def records(self) -> builtins.list[dict]:
        r"""
        Return all audit records as a list of dicts.

        Each dict has keys: ``action``, ``success``, ``error`` (str|None),
        ``output_preview`` (str|None), ``timestamp_ms`` (int).
        """
    def records_for_action(self, action: builtins.str) -> builtins.list[dict]:
        r"""
        Return audit records for a specific action name.
        """
    def record_count(self) -> builtins.int:
        r"""
        Return the number of recorded audit entries.
        """
    def clear(self) -> None:
        r"""
        Clear all audit records.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class BoundingBox:
    r"""
    Python-facing axis-aligned bounding box.

    ```python
    from dcc_mcp_core import BoundingBox

    bb = BoundingBox(min=[-1.0, 0.0, -1.0], max=[1.0, 2.0, 1.0])
    print(bb.center())  # [0.0, 1.0, 0.0]
    ```
    """
    @property
    def min(self) -> builtins.list[builtins.float]:
        r"""
        Minimum corner [x, y, z] in world space (centimeters).
        """
    @min.setter
    def min(self, value: typing.Sequence[builtins.float]) -> None:
        r"""
        Minimum corner [x, y, z] in world space (centimeters).
        """
    @property
    def max(self) -> builtins.list[builtins.float]:
        r"""
        Maximum corner [x, y, z] in world space (centimeters).
        """
    @max.setter
    def max(self, value: typing.Sequence[builtins.float]) -> None:
        r"""
        Maximum corner [x, y, z] in world space (centimeters).
        """

@typing.final
class CaptureBackendKind:
    r"""
    Python wrapper for [`CaptureBackendKind`].
    """
    DxgiDesktopDuplication: CaptureBackendKind
    WindowsGraphicsCapture: CaptureBackendKind
    ScreenCaptureKit: CaptureBackendKind
    X11Xshm: CaptureBackendKind
    PipeWire: CaptureBackendKind
    Mock: CaptureBackendKind
    @property
    def name(self) -> builtins.str:
        r"""
        Human-readable backend name.
        """
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class CaptureFrame:
    r"""
    A single captured frame (Python-visible).
    """
    ...

@typing.final
class CaptureResult:
    r"""
    Python-facing capture/screenshot result.
    """
    @property
    def data(self) -> builtins.bytes: ...
    @property
    def width(self) -> builtins.int: ...
    @property
    def height(self) -> builtins.int: ...
    @property
    def format(self) -> builtins.str: ...
    @property
    def viewport(self) -> typing.Optional[builtins.str]: ...
    def __new__(cls, data: builtins.bytes, width: builtins.int, height: builtins.int, format: builtins.str, viewport: typing.Optional[builtins.str] = None) -> CaptureResult: ...
    def data_size(self) -> builtins.int:
        r"""
        Get the image data size in bytes.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class CaptureTarget:
    r"""
    Opaque Python wrapper for [`CaptureTarget`].

    Construct via the ``primary_display``, ``monitor_index``, ``process_id``,
    ``window_title`` or ``window_handle`` static factories.
    """
    @staticmethod
    def primary_display() -> CaptureTarget: ...
    @staticmethod
    def monitor_index(index: builtins.int) -> CaptureTarget: ...
    @staticmethod
    def process_id(pid: builtins.int) -> CaptureTarget: ...
    @staticmethod
    def window_title(title: builtins.str) -> CaptureTarget: ...
    @staticmethod
    def window_handle(handle: builtins.int) -> CaptureTarget: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class Capturer:
    r"""
    DCC screenshot / frame-capture entry point (Python-visible).

    # Example (Python)
    ```python
    from dcc_mcp_core import PyCapturer
    capturer = PyCapturer.new_auto()
    frame = capturer.capture()
    print(f"Captured {frame.width}×{frame.height}, {frame.byte_len()} bytes")
    ```
    """
    @staticmethod
    def new_auto() -> Capturer:
        r"""
        Create a capturer using the best available backend on this platform.
        """
    @staticmethod
    def new_window_auto() -> Capturer:
        r"""
        Create a capturer configured for single-window capture.

        Uses Windows.Graphics.Capture with HWND fallback on Windows; falls back
        to Mock on other platforms until window-target backends are added.
        """
    @staticmethod
    def new_mock(width: builtins.int = 1920, height: builtins.int = 1080) -> Capturer:
        r"""
        Create a capturer backed by the mock (synthetic checkerboard) backend.

        Useful in headless CI and testing without a GPU or display.
        """
    def capture(self, format: builtins.str = 'png', jpeg_quality: builtins.int = 85, scale: builtins.float = 1.0, timeout_ms: builtins.int = 5000, process_id: typing.Optional[builtins.int] = None, window_title: typing.Optional[builtins.str] = None) -> CaptureFrame:
        r"""
        Capture a single frame.

        Parameters
        ----------
        format : str, optional
            ``"png"`` (default), ``"jpeg"``, or ``"raw_bgra"``.
        jpeg_quality : int, optional
            JPEG quality 0-100 (default 85). Ignored for PNG / raw_bgra.
        scale : float, optional
            Scale factor 0.0-1.0 (default 1.0 = native resolution).
        timeout_ms : int, optional
            Max milliseconds to wait for a frame (default 5000).
        process_id : int, optional
            Capture the window belonging to this PID.
        window_title : str, optional
            Capture the window whose title contains this substring.
        """
    def capture_window(self, *, process_id: typing.Optional[builtins.int] = None, window_handle: typing.Optional[builtins.int] = None, window_title: typing.Optional[builtins.str] = None, format: builtins.str = 'png', jpeg_quality: builtins.int = 85, scale: builtins.float = 1.0, timeout_ms: builtins.int = 5000, include_decorations: builtins.bool = True) -> CaptureFrame:
        r"""
        Capture a single window.

        Parameters
        ----------
        process_id : int, optional
        window_handle : int, optional
        window_title : str, optional
            At least one of the three must be provided.
        format, jpeg_quality, scale, timeout_ms, include_decorations :
            Same semantics as :py:meth:`capture`.
        """
    def backend_name(self) -> builtins.str:
        r"""
        Return the active backend name.
        """
    def backend_kind(self) -> CaptureBackendKind:
        r"""
        Return the active backend kind enum.
        """
    def stats(self) -> tuple[builtins.int, builtins.int, builtins.int]:
        r"""
        Return capture statistics as ``(count, total_bytes, errors)``.
        """
    @staticmethod
    def capture_window_png(pid: builtins.int, *, timeout_ms: builtins.int = 1000) -> typing.Any:
        r"""
        Capture a single window as PNG bytes, looked up by process ID.

        This is a sugar wrapper intended for the common "grab a snapshot of a
        DCC window, attach it to a chat message" workflow — no ``Capturer``
        instance needed.  Internally creates a window-auto capturer, captures,
        and returns the PNG-encoded bytes.

        Parameters
        ----------
        pid : int
            OS process ID of the DCC to capture.
        timeout_ms : int, optional
            Max milliseconds to wait for the frame (default 1000).

        Returns
        -------
        bytes | None
            PNG-encoded bytes on success; ``None`` when the process has no
            visible top-level window or the backend is unavailable (the
            function never raises for capture errors — use the instance
            :py:meth:`capture_window` API when you need exceptions).
        """
    @staticmethod
    def capture_region_png(pid: builtins.int, x: builtins.int, y: builtins.int, w: builtins.int, h: builtins.int, *, timeout_ms: builtins.int = 1000) -> typing.Any:
        r"""
        Capture a cropped rectangle of a window as PNG bytes, looked up by PID.

        The window is captured first (via the same backend as
        :py:meth:`capture_window_png`) and the ``(x, y, w, h)`` region is
        cropped in CPU before re-encoding.  Coordinates are in window-local
        pixels relative to the top-left of the window rectangle.

        Parameters
        ----------
        pid, x, y, w, h : int
        timeout_ms : int, optional (default 1000)

        Returns
        -------
        bytes | None
            PNG-encoded cropped bytes on success; ``None`` on any failure
            (window not found, crop out of bounds, decode error, ...).
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class DccCapabilities:
    r"""
    Python-facing DCC capabilities declaration.
    """
    @property
    def script_languages(self) -> builtins.list[ScriptLanguage]: ...
    @script_languages.setter
    def script_languages(self, value: typing.Sequence[ScriptLanguage]) -> None: ...
    @property
    def scene_info(self) -> builtins.bool: ...
    @scene_info.setter
    def scene_info(self, value: builtins.bool) -> None: ...
    @property
    def snapshot(self) -> builtins.bool: ...
    @snapshot.setter
    def snapshot(self, value: builtins.bool) -> None: ...
    @property
    def undo_redo(self) -> builtins.bool: ...
    @undo_redo.setter
    def undo_redo(self, value: builtins.bool) -> None: ...
    @property
    def progress_reporting(self) -> builtins.bool: ...
    @progress_reporting.setter
    def progress_reporting(self, value: builtins.bool) -> None: ...
    @property
    def file_operations(self) -> builtins.bool: ...
    @file_operations.setter
    def file_operations(self, value: builtins.bool) -> None: ...
    @property
    def selection(self) -> builtins.bool: ...
    @selection.setter
    def selection(self, value: builtins.bool) -> None: ...
    @property
    def scene_manager(self) -> builtins.bool:
        r"""
        Whether the adapter implements `DccSceneManager` (scene/file management).
        """
    @scene_manager.setter
    def scene_manager(self, value: builtins.bool) -> None:
        r"""
        Whether the adapter implements `DccSceneManager` (scene/file management).
        """
    @property
    def transform(self) -> builtins.bool:
        r"""
        Whether the adapter implements `DccTransform` (object TRS transforms).
        """
    @transform.setter
    def transform(self, value: builtins.bool) -> None:
        r"""
        Whether the adapter implements `DccTransform` (object TRS transforms).
        """
    @property
    def render_capture(self) -> builtins.bool:
        r"""
        Whether the adapter implements `DccRenderCapture` (viewport capture + render).
        """
    @render_capture.setter
    def render_capture(self, value: builtins.bool) -> None:
        r"""
        Whether the adapter implements `DccRenderCapture` (viewport capture + render).
        """
    @property
    def hierarchy(self) -> builtins.bool:
        r"""
        Whether the adapter implements `DccHierarchy` (parent/child hierarchy).
        """
    @hierarchy.setter
    def hierarchy(self, value: builtins.bool) -> None:
        r"""
        Whether the adapter implements `DccHierarchy` (parent/child hierarchy).
        """
    @property
    def has_embedded_python(self) -> builtins.bool:
        r"""
        Whether the DCC has an embedded Python interpreter.
        `False` for bridge-based DCCs (ZBrush, Photoshop).
        """
    @has_embedded_python.setter
    def has_embedded_python(self, value: builtins.bool) -> None:
        r"""
        Whether the DCC has an embedded Python interpreter.
        `False` for bridge-based DCCs (ZBrush, Photoshop).
        """
    @property
    def bridge_kind(self) -> typing.Optional[builtins.str]:
        r"""
        Bridge kind string: `"http"`, `"websocket"`, `"named_pipe"`, or `None`.
        """
    @bridge_kind.setter
    def bridge_kind(self, value: typing.Optional[builtins.str]) -> None:
        r"""
        Bridge kind string: `"http"`, `"websocket"`, `"named_pipe"`, or `None`.
        """
    @property
    def bridge_endpoint(self) -> typing.Optional[builtins.str]:
        r"""
        Bridge endpoint (URL or socket path).
        """
    @bridge_endpoint.setter
    def bridge_endpoint(self, value: typing.Optional[builtins.str]) -> None:
        r"""
        Bridge endpoint (URL or socket path).
        """
    @property
    def extensions(self) -> builtins.dict[builtins.str, builtins.bool]: ...
    @extensions.setter
    def extensions(self, value: typing.Mapping[builtins.str, builtins.bool]) -> None: ...
    def __new__(cls, script_languages: typing.Sequence[ScriptLanguage] = [], scene_info: builtins.bool = False, snapshot: builtins.bool = False, undo_redo: builtins.bool = False, progress_reporting: builtins.bool = False, file_operations: builtins.bool = False, selection: builtins.bool = False, scene_manager: builtins.bool = False, transform: builtins.bool = False, render_capture: builtins.bool = False, hierarchy: builtins.bool = False, has_embedded_python: builtins.bool = True, bridge_kind: typing.Optional[builtins.str] = None, bridge_endpoint: typing.Optional[builtins.str] = None, extensions: typing.Optional[typing.Mapping[builtins.str, builtins.bool]] = None) -> DccCapabilities: ...
    def __repr__(self) -> builtins.str: ...
    def uses_bridge(self) -> builtins.bool:
        r"""
        Check if this DCC uses a bridge connection (i.e., no embedded Python).

        Returns ``True`` if ``bridge_kind`` is set (http, websocket, named_pipe, or custom).
        """
    @staticmethod
    def http_bridge(endpoint: builtins.str) -> DccCapabilities:
        r"""
        Create a DccCapabilities for an HTTP bridge-based DCC.

        Args:
            endpoint: HTTP endpoint URL (e.g., ``"http://localhost:8765"``).

        Example::

            caps = DccCapabilities.http_bridge("http://localhost:8765")
            assert caps.bridge_kind == "http"
            assert caps.uses_bridge()
        """
    @staticmethod
    def websocket_bridge(endpoint: builtins.str) -> DccCapabilities:
        r"""
        Create a DccCapabilities for a WebSocket bridge-based DCC.

        Args:
            endpoint: WebSocket endpoint URL (e.g., ``"ws://localhost:9001"``).

        Example::

            caps = DccCapabilities.websocket_bridge("ws://localhost:9001")
            assert caps.bridge_kind == "websocket"
            assert caps.uses_bridge()
        """

@typing.final
class DccError:
    r"""
    Python-facing DCC error.
    """
    @property
    def code(self) -> DccErrorCode: ...
    @property
    def message(self) -> builtins.str: ...
    @property
    def details(self) -> typing.Optional[builtins.str]: ...
    @property
    def recoverable(self) -> builtins.bool: ...
    def __new__(cls, code: DccErrorCode, message: builtins.str, details: typing.Optional[builtins.str] = None, recoverable: builtins.bool = False) -> DccError: ...
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class DccInfo:
    r"""
    Python-facing DCC application information.

    ```python
    from dcc_mcp_core import DccInfo

    info = DccInfo(
        dcc_type="maya",
        version="2024.2",
        platform="windows",
        pid=12345,
        python_version="3.10.11",
    )
    print(info.dcc_type)  # "maya"
    ```
    """
    @property
    def dcc_type(self) -> builtins.str: ...
    @property
    def version(self) -> builtins.str: ...
    @property
    def python_version(self) -> typing.Optional[builtins.str]: ...
    @property
    def platform(self) -> builtins.str: ...
    @property
    def pid(self) -> builtins.int: ...
    @property
    def metadata(self) -> builtins.dict[builtins.str, builtins.str]: ...

@typing.final
class DccLinkFrame:
    r"""
    A DCC-Link frame with ``version``, ``msg_type``, ``seq``, and ``body`` fields.

    Args:
        msg_type: Integer message type tag (1=Call, 2=Reply, 3=Err,
                  4=Progress, 5=Cancel, 6=Push, 7=Ping, 8=Pong).
        seq:      Sequence number (uint64).
        body:     Payload bytes.
        version:  Wire protocol version. Defaults to the current version; use
                  0 only while communicating with a legacy peer.
    """
    @property
    def version(self) -> builtins.int:
        r"""
        DCC-Link wire protocol version (``0`` identifies a legacy frame).
        """
    @property
    def msg_type(self) -> builtins.int:
        r"""
        Message type tag (1=Call, 2=Reply, 3=Err, 4=Progress, 5=Cancel,
        6=Push, 7=Ping, 8=Pong).
        """
    @property
    def seq(self) -> builtins.int:
        r"""
        Sequence number.
        """
    @property
    def body(self) -> builtins.bytes:
        r"""
        Payload bytes.
        """
    def __new__(cls, msg_type: builtins.int, seq: builtins.int, body: typing.Optional[builtins.bytes] = None, version: builtins.int = 1) -> DccLinkFrame: ...
    def encode(self) -> builtins.bytes:
        r"""
        Encode the frame to bytes (``[len][version tag][type][seq][body]``).
        """
    @staticmethod
    def decode(data: builtins.bytes) -> DccLinkFrame:
        r"""
        Decode a frame from bytes including the 4-byte length prefix.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class EventBus:
    r"""
    Thread-safe event bus.
    """
    def __new__(cls) -> EventBus: ...
    def subscribe(self, event_name: builtins.str, callback: typing.Any) -> builtins.int:
        r"""
        Subscribe to an event. Returns subscriber ID for unsubscription.
        """
    def before(self, event_name: builtins.str, callback: typing.Any) -> builtins.int:
        r"""
        Register a before hook for a vetoable event.

        The callback receives the structured event envelope. Return ``None`` or
        ``False`` to allow the operation, or return ``EventBus.veto(...)``, a
        ``{"reason": "...", "code": "..."}`` dict, or a string reason to veto.
        """
    def unsubscribe(self, event_name: builtins.str, subscriber_id: builtins.int) -> builtins.bool:
        r"""
        Unsubscribe from an event by subscriber ID.
        """
    def unsubscribe_before(self, event_name: builtins.str, subscriber_id: builtins.int) -> builtins.bool:
        r"""
        Unsubscribe a before hook by subscriber ID.
        """
    @staticmethod
    def veto(reason: builtins.str, code: builtins.str = 'vetoed') -> typing.Any:
        r"""
        Return a veto payload for before-hook callbacks.
        """
    @staticmethod
    def vetoable_events() -> builtins.list[builtins.str]:
        r"""
        Return the event names that accept before hooks.
        """
    def publish(self, event_name: builtins.str, **kwargs: typing.Any) -> None:
        r"""
        Publish an event with legacy keyword payloads, calling all subscribers.

        Callbacks are collected before invocation to avoid holding a DashMap
        read-lock while executing user code (which could attempt to
        subscribe/unsubscribe, causing a deadlock on the same shard).
        """
    def emit(self, event_name: builtins.str, source: typing.Optional[typing.Any] = None, correlation: typing.Optional[typing.Any] = None, attributes: typing.Optional[typing.Any] = None) -> typing.Any:
        r"""
        Emit a structured RFC-0002 event envelope.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class FileRef:
    r"""
    Python wrapper for [`crate::FileRef`].
    """
    @property
    def uri(self) -> builtins.str:
        r"""
        Canonical URI, e.g. ``artefact://sha256/<hex>``.
        """
    @property
    def mime(self) -> typing.Optional[builtins.str]:
        r"""
        Optional MIME type (``image/png``, ``application/json``, …).
        """
    @property
    def size_bytes(self) -> typing.Optional[builtins.int]:
        r"""
        Size of the artefact body in bytes, if known.
        """
    @property
    def display_name(self) -> typing.Optional[builtins.str]:
        r"""
        Optional display filename/name for clients.
        """
    @property
    def digest(self) -> typing.Optional[builtins.str]:
        r"""
        Canonical digest, e.g. ``sha256:<hex>``.
        """
    @property
    def producer_job_id(self) -> typing.Optional[builtins.str]:
        r"""
        UUID of the job that produced the artefact (when known).
        """
    @property
    def tool_call_id(self) -> typing.Optional[builtins.str]:
        r"""
        Tool call/request id that produced the artefact (when known).
        """
    @property
    def session_id(self) -> typing.Optional[builtins.str]:
        r"""
        Session id that produced the artefact (when known).
        """
    @property
    def correlation_id(self) -> typing.Optional[builtins.str]:
        r"""
        Adapter-defined correlation id (when known).
        """
    @property
    def created_at(self) -> builtins.str:
        r"""
        RFC-3339 creation timestamp.
        """
    @property
    def expires_at(self) -> typing.Optional[builtins.str]:
        r"""
        RFC-3339 expiry timestamp when retention is configured.
        """
    @property
    def metadata_json(self) -> builtins.str:
        r"""
        Tool-defined metadata as a JSON string.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class FrameRange:
    r"""
    Python-facing frame range and timing information.

    ```python
    from dcc_mcp_core import FrameRange

    fr = FrameRange(start=1.0, end=240.0, fps=24.0, current=1.0)
    print(fr.end - fr.start)  # 239.0
    ```
    """
    @property
    def start(self) -> builtins.float: ...
    @start.setter
    def start(self, value: builtins.float) -> None: ...
    @property
    def end(self) -> builtins.float: ...
    @end.setter
    def end(self, value: builtins.float) -> None: ...
    @property
    def fps(self) -> builtins.float: ...
    @fps.setter
    def fps(self, value: builtins.float) -> None: ...
    @property
    def current(self) -> builtins.float: ...
    @current.setter
    def current(self, value: builtins.float) -> None: ...

@typing.final
class GracefulIpcChannelAdapter:
    r"""
    Graceful IPC channel adapter with shutdown support.

    Extends :class:`IpcChannelAdapter` with graceful shutdown. For
    reentrancy-safe dispatch (``bind_affinity_thread``, ``submit``,
    ``pump_pending``), use the Rust-level ``GracefulIpcChannelAdapter``
    directly or the ``DeferredExecutor`` from the Python ``_core`` module.
    """
    @staticmethod
    def create(name: builtins.str) -> GracefulIpcChannelAdapter:
        r"""
        Create a server-side graceful IPC channel.
        """
    @staticmethod
    def connect(name: builtins.str) -> GracefulIpcChannelAdapter:
        r"""
        Connect to an existing graceful IPC channel.
        """
    def wait_for_client(self) -> None:
        r"""
        Wait for a client to connect (server-side only).
        """
    def send_frame(self, frame: DccLinkFrame) -> None:
        r"""
        Send a DCC-Link frame to the peer.
        """
    def recv_frame(self) -> typing.Optional[DccLinkFrame]:
        r"""
        Receive a DCC-Link frame from the peer (blocking).

        Returns:
            A :class:`DccLinkFrame`, or ``None`` if the channel is closed.
        """
    def shutdown(self) -> None:
        r"""
        Signal the channel to shut down gracefully.
        """
    def bind_affinity_thread(self) -> None:
        r"""
        Bind the current thread as the affinity thread for reentrancy-safe
        dispatch. Call this **once** on the DCC main thread.

        This is a low-level method; for most Python use cases, prefer
        ``DeferredExecutor`` from ``dcc_mcp_core._core``.
        """
    def pump_pending(self, budget_ms: builtins.int = 100) -> builtins.int:
        r"""
        Drain pending work items on the affinity thread within the given
        budget. Returns the number of items processed.

        Call from the DCC host's idle callback (e.g. Maya ``scriptJob
        idleEvent``, Blender ``bpy.app.timers``).

        Args:
            budget_ms: Budget in milliseconds. Defaults to 100.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class GuiExecutableHint:
    r"""
    Python-visible mirror of [`GuiExecutableHint`].

    Constructed only by `is_gui_executable(...)`; not user-instantiable.
    """
    @property
    def gui_path(self) -> pathlib.Path:
        r"""
        The path that was probed.
        """
    @property
    def dcc_kind(self) -> builtins.str:
        r"""
        DCC family name (`"maya"`, `"houdini"`, `"unreal"`, …).
        """
    @property
    def recommended_replacement(self) -> typing.Optional[pathlib.Path]:
        r"""
        Recommended replacement path resolved from a sibling binary, or
        `None` when no headless equivalent exists / the sibling cannot
        be located on disk.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class InputValidator:
    r"""
    Python-accessible input validator.

    Example::

        v = PyInputValidator()
        v.require_string("name", max_length=50)
        v.require_number("count", min_value=0, max_value=1000)
        ok, error = v.validate('{"name": "sphere", "count": 5}')
    """
    def __new__(cls) -> InputValidator: ...
    def require_string(self, field: builtins.str, max_length: typing.Optional[builtins.int], min_length: typing.Optional[builtins.int]) -> None:
        r"""
        Add a required string field with optional length constraints.
        """
    def require_number(self, field: builtins.str, min_value: typing.Optional[builtins.float], max_value: typing.Optional[builtins.float]) -> None:
        r"""
        Add a required numeric field with optional range constraints.
        """
    def forbid_substrings(self, field: builtins.str, substrings: typing.Sequence[builtins.str]) -> None:
        r"""
        Add injection-guard for a string field.
        """
    def validate(self, params_json: builtins.str) -> tuple[builtins.bool, typing.Optional[builtins.str]]:
        r"""
        Validate a JSON string params payload.

        Returns ``(True, None)`` on success, ``(False, error_message)`` on failure.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class InstanceStatus:
    r"""
    Python-facing unified instance status (ADR 018).

    Combines transport-level status, dispatch readiness, retryability,
    and a recommended next action.

    ```python,ignore
    from dcc_mcp_core import InstanceStatus

    status = InstanceStatus.from_entry(entry, stale=False)
    print(status.status)               # ServiceStatus.AVAILABLE
    print(status.dispatch_status)      # DispatchStatus.READY
    print(status.retryable)            # True
    print(status.recommended_next_action)  # "Instance is available for dispatch."
    ```
    """
    @property
    def status(self) -> ServiceStatus:
        r"""
        Transport-level connection state.
        """
    @property
    def dispatch_status(self) -> DispatchStatus:
        r"""
        Application-level dispatch readiness.
        """
    @property
    def retryable(self) -> builtins.bool:
        r"""
        Whether the current state is safe to retry.
        """
    @property
    def recommended_next_action(self) -> builtins.str:
        r"""
        Human + machine-readable recommended next step.
        """

@typing.final
class IpcChannelAdapter:
    r"""
    Thin adapter over ``ipckit::IpcChannel`` using DCC-Link framing.

    Create a server-side channel with :meth:`create` or connect as a client
    with :meth:`connect`.
    """
    @staticmethod
    def create(name: builtins.str) -> IpcChannelAdapter:
        r"""
        Create a server-side IPC channel with the given name.

        Args:
            name: Channel name (used as the IPC endpoint identifier).

        Returns:
            A new :class:`IpcChannelAdapter` in server mode.
        """
    @staticmethod
    def connect(name: builtins.str) -> IpcChannelAdapter:
        r"""
        Connect to an existing IPC channel by name.

        Args:
            name: Channel name to connect to.

        Returns:
            A new :class:`IpcChannelAdapter` in client mode.
        """
    def wait_for_client(self) -> None:
        r"""
        Wait for a client to connect (server-side only).
        """
    def send_frame(self, frame: DccLinkFrame) -> None:
        r"""
        Send a DCC-Link frame to the peer.

        Args:
            frame: The :class:`DccLinkFrame` to send.
        """
    def recv_frame(self) -> typing.Optional[DccLinkFrame]:
        r"""
        Receive a DCC-Link frame from the peer (blocking).

        Returns:
            A :class:`DccLinkFrame`, or ``None`` if the channel is closed.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class LoggingMiddleware:
    r"""
    Logging middleware — emits tracing log lines before/after each action.

    ```python
    from dcc_mcp_core import ToolRegistry, ToolDispatcher, ToolPipeline, LoggingMiddleware

    reg = ToolRegistry()
    dispatcher = ToolDispatcher(reg)
    pipeline = ToolPipeline(dispatcher)
    pipeline.add_logging(log_params=True)
    ```
    """
    @property
    def log_params(self) -> builtins.bool:
        r"""
        Whether parameters are logged.
        """
    def __new__(cls, log_params: builtins.bool = False) -> LoggingMiddleware:
        r"""
        Create a new logging middleware.

        Args:
            log_params: If True, also log the action parameters (default: False).
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class McpServerHandle:
    r"""
    Handle returned by `McpHttpServer.start()`.

    Example::

        handle = server.start()
        # ... later ...
        handle.shutdown()
    """
    @property
    def port(self) -> builtins.int:
        r"""
        The actual port the server is listening on.
        """
    @property
    def bind_addr(self) -> builtins.str:
        r"""
        The bind address (e.g. ``127.0.0.1:8765``).
        """
    @property
    def is_gateway(self) -> builtins.bool:
        r"""
        ``True`` if this process won the gateway port competition.
        """
    @property
    def instance_id(self) -> typing.Optional[builtins.str]:
        r"""
        The exact UUID registered in ``FileRegistry``, or ``None``.

        ``None`` means gateway registration was disabled or unavailable. The
        value is cached on the handle and remains readable after ``shutdown()``.
        """
    def mcp_url(self) -> builtins.str:
        r"""
        The full MCP endpoint URL.
        """
    def shutdown(self) -> None:
        r"""
        Gracefully shut down the server.
        """
    def __enter__(self) -> McpServerHandle: ...
    def __exit__(self, _exc_type: typing.Any, _exc: typing.Any, _tb: typing.Any) -> None: ...
    def signal_shutdown(self) -> None:
        r"""
        Signal shutdown without blocking.
        """
    def update_scene(self, scene: typing.Optional[builtins.str] = None, version: typing.Optional[builtins.str] = None, documents: typing.Optional[typing.Sequence[builtins.str]] = None, display_name: typing.Optional[builtins.str] = None) -> None:
        r"""
        Update the live instance metadata in the gateway registry.

        Works for both single-document DCCs (Maya, Blender — pass ``scene``
        only) and multi-document DCCs (Photoshop, After Effects — also pass
        ``documents`` with the full list of open files and optionally
        ``display_name`` to label the instance).

        Values are written into the shared live-metadata store and propagated
        to ``FileRegistry`` on the next heartbeat tick (≤ 5 s).  After the
        update, ``list_dcc_instances`` reflects the change so AI agents and
        users can identify the correct instance without restarting.

        Pass ``None`` to leave a field unchanged; pass ``""`` / ``[]`` to
        clear it.

        Examples::

            # Maya — single active scene:
            handle.update_scene("C:/projects/hero/rig.ma")

            # Photoshop — active document + all open docs + instance label:
            handle.update_scene(
                scene="hero_comp.psd",
                documents=["hero_comp.psd", "bg_plate.psd", "overlay.psd"],
                display_name="PS-Marketing",
            )

            # Clear the document list (single-doc mode again):
            handle.update_scene(documents=[])

        Args:
            scene: Active/focused scene or document path.
                    ``None`` = no change, ``""`` = clear.
            version: DCC application version string.
                     ``None`` = no change, ``""`` = clear.
            documents: Full list of open documents (multi-doc DCCs).
                       ``None`` = no change, ``[]`` = clear list.
            display_name: Human-readable instance label (e.g. ``"PS-Marketing"``).
                          ``None`` = no change, ``""`` = clear.
        """
    def update_gateway_metadata(self, metadata: typing.Mapping[builtins.str, builtins.str]) -> None:
        r"""
        Merge arbitrary string metadata into the FileRegistry row.

        Values propagate on the next heartbeat tick. Empty values clear the
        matching key while preserving unrelated metadata.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ObjectTransform:
    r"""
    Python-facing 3D object transform (TRS).

    ```python
    from dcc_mcp_core import ObjectTransform

    t = ObjectTransform(translate=[0.0, 10.0, 0.0], rotate=[0.0, 45.0, 0.0], scale=[1.0, 1.0, 1.0])
    print(t.translate)  # [0.0, 10.0, 0.0]
    ```
    """
    @property
    def translate(self) -> builtins.list[builtins.float]:
        r"""
        World-space translation [x, y, z] in centimeters.
        """
    @translate.setter
    def translate(self, value: typing.Sequence[builtins.float]) -> None:
        r"""
        World-space translation [x, y, z] in centimeters.
        """
    @property
    def rotate(self) -> builtins.list[builtins.float]:
        r"""
        Euler XYZ rotation in degrees [rx, ry, rz].
        """
    @rotate.setter
    def rotate(self, value: typing.Sequence[builtins.float]) -> None:
        r"""
        Euler XYZ rotation in degrees [rx, ry, rz].
        """
    @property
    def scale(self) -> builtins.list[builtins.float]:
        r"""
        Scale [sx, sy, sz].
        """
    @scale.setter
    def scale(self, value: typing.Sequence[builtins.float]) -> None:
        r"""
        Scale [sx, sy, sz].
        """

@typing.final
class PromptArgument:
    r"""
    MCP Prompt argument.

    Describes a single named argument that a prompt accepts, including
    whether it is required or optional.
    """
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def description(self) -> builtins.str: ...
    @description.setter
    def description(self, value: builtins.str) -> None: ...
    @property
    def required(self) -> builtins.bool: ...
    @required.setter
    def required(self, value: builtins.bool) -> None: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, name: builtins.str, description: builtins.str, required: builtins.bool = False) -> PromptArgument: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class PromptDefinition:
    r"""
    MCP Prompt definition.

    Per MCP spec (2025-11-25), a prompt MAY declare typed arguments that
    the client should collect before invoking the prompt.
    """
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def description(self) -> builtins.str: ...
    @description.setter
    def description(self, value: builtins.str) -> None: ...
    @property
    def arguments(self) -> builtins.list[PromptArgument]:
        r"""
        Optional list of arguments the prompt accepts.
        """
    @arguments.setter
    def arguments(self, value: typing.Sequence[PromptArgument]) -> None:
        r"""
        Optional list of arguments the prompt accepts.
        """
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, name: builtins.str, description: builtins.str, arguments: typing.Sequence[PromptArgument] = []) -> PromptDefinition: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyBufferPool:
    r"""
    A fixed-capacity pool of reusable shared memory buffers.

    Usage::

        pool = PyBufferPool(capacity=4, buffer_size=1024 * 1024)
        buf = pool.acquire()
        buf.write(b"scene snapshot")
        # buf automatically returned on GC / explicit del
    """
    def __new__(cls, capacity: builtins.int, buffer_size: builtins.int) -> PyBufferPool: ...
    def acquire(self) -> PySharedBuffer:
        r"""
        Acquire a free buffer.  Raises ``RuntimeError`` if all slots are in use.

        The pool slot remains marked as in-use until the returned buffer object
        is garbage-collected by Python.
        """
    def available(self) -> builtins.int:
        r"""
        Number of currently available (free) slots.
        """
    def capacity(self) -> builtins.int:
        r"""
        Total pool capacity.
        """
    def buffer_size(self) -> builtins.int:
        r"""
        Per-buffer size in bytes.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyCrashRecoveryPolicy:
    r"""
    Crash recovery policy for DCC processes.

    # Example (Python)

    ```python
    policy = PyCrashRecoveryPolicy(max_restarts=3)
    policy.use_exponential_backoff(initial_ms=1000, max_delay_ms=30000)
    print(policy.should_restart("crashed"))   # True
    print(policy.next_delay_ms("maya", 0))    # 1000
    ```
    """
    @property
    def max_restarts(self) -> builtins.int:
        r"""
        Maximum number of restart attempts.
        """
    def __new__(cls, max_restarts: builtins.int = 3) -> PyCrashRecoveryPolicy:
        r"""
        Create a policy with ``max_restarts`` and fixed 2 s back-off by default.
        """
    def use_exponential_backoff(self, initial_ms: builtins.int, max_delay_ms: builtins.int) -> None:
        r"""
        Switch to exponential back-off.
        """
    def use_fixed_backoff(self, delay_ms: builtins.int) -> None:
        r"""
        Switch to fixed back-off.
        """
    def should_restart(self, status: builtins.str) -> builtins.bool:
        r"""
        Returns `True` if the given status string warrants a restart.

        Recognised status values: ``"crashed"``, ``"unresponsive"``.
        Always returns `False` when `max_restarts` is 0.
        """
    def next_delay_ms(self, name: builtins.str, attempt: builtins.int) -> builtins.int:
        r"""
        Return the delay (ms) before attempt ``attempt`` (0-indexed), or raise
        `RuntimeError` if `max_restarts` has been exceeded.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyDccLauncher:
    r"""
    Async DCC process launcher (spawn / terminate / kill).

    # Example (Python)

    ```python
    launcher = PyDccLauncher()
    # info = launcher.launch("maya-2025", "/usr/autodesk/maya/bin/maya", [], 30000)
    ```
    """
    def __new__(cls) -> PyDccLauncher: ...
    def launch(self, name: builtins.str, executable: builtins.str, args: typing.Optional[typing.Sequence[builtins.str]] = None, launch_timeout_ms: builtins.int = 30000, environment: typing.Optional[typing.Mapping[builtins.str, builtins.str]] = None, working_directory: typing.Optional[builtins.str] = None) -> dict:
        r"""
        Spawn a DCC process.

        Parameters
        ----------
        name : str
            Logical name for this DCC instance.
        executable : str
            Full path to the DCC executable.
        args : list[str], optional
            Command-line arguments.
        launch_timeout_ms : int, optional
            Milliseconds to wait for the process to start (default 30000).
        environment : dict[str, str], optional
            Child-only environment overrides. The parent process is not mutated.
        working_directory : str, optional
            Working directory for the child process.

        Returns a dict with ``pid``, ``name``, and ``status``.
        """
    def terminate(self, name: builtins.str, timeout_ms: builtins.int = 5000) -> None:
        r"""
        Gracefully terminate the named process.

        Parameters
        ----------
        name : str
            The logical name used at launch time.
        timeout_ms : int, optional
            Milliseconds to wait for the process to exit (default 5000).
        """
    def kill(self, name: builtins.str) -> None:
        r"""
        Forcefully kill the named process.
        """
    def pid_of(self, name: builtins.str) -> typing.Optional[builtins.int]:
        r"""
        Return the PID of the named running child, or `None`.
        """
    def running_count(self) -> builtins.int:
        r"""
        Return the number of currently tracked live children.
        """
    def restart_count(self, name: builtins.str) -> builtins.int:
        r"""
        Return the restart count for the given name.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyProcessMonitor:
    r"""
    Cross-platform DCC process monitor.

    Wraps `ProcessMonitor` — tracks live resource snapshots via `sysinfo`.

    # Example (Python)

    ```python
    mon = PyProcessMonitor()
    import os
    mon.track(os.getpid(), "self")
    mon.refresh()
    info = mon.query(os.getpid())
    print(info["status"])  # "running"
    ```
    """
    def __new__(cls) -> PyProcessMonitor: ...
    def track(self, pid: builtins.int, name: builtins.str) -> None:
        r"""
        Register a PID to monitor.
        """
    def untrack(self, pid: builtins.int) -> None:
        r"""
        Stop monitoring a PID.
        """
    def refresh(self) -> None:
        r"""
        Refresh underlying system data.  Must be called before querying.
        """
    def query(self, pid: builtins.int) -> typing.Optional[dict]:
        r"""
        Return a dict snapshot for `pid`, or `None` if not found.
        """
    def list_all(self) -> builtins.list[dict]:
        r"""
        Return snapshots for all tracked PIDs.
        """
    def is_alive(self, pid: builtins.int) -> builtins.bool:
        r"""
        Returns `True` if `pid` is present in the OS process table.

        Performs a fresh OS query for the given PID so `track()` does not need
        to be called first.
        """
    def tracked_count(self) -> builtins.int:
        r"""
        Return the number of currently tracked PIDs.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyProcessWatcher:
    r"""
    Asynchronous background process watcher with event polling.

    Spawns a background loop that periodically polls tracked processes and
    collects events.  Python consumers can call `poll_events()` to drain the
    event queue.

    # Example (Python)

    ```python
    import os, time
    watcher = PyProcessWatcher(poll_interval_ms=200)
    watcher.track(os.getpid(), "self")
    watcher.start()
    time.sleep(0.5)
    events = watcher.poll_events()
    print(events[0]["type"])  # "heartbeat"
    watcher.stop()
    ```
    """
    def __new__(cls, poll_interval_ms: builtins.int = 500) -> PyProcessWatcher:
        r"""
        Create a new watcher that polls every `poll_interval_ms` milliseconds.
        """
    def track(self, pid: builtins.int, name: builtins.str) -> None:
        r"""
        Register a PID to monitor.
        """
    def untrack(self, pid: builtins.int) -> None:
        r"""
        Stop monitoring a PID.
        """
    def add_watch(self, pid: builtins.int, name: builtins.str) -> None:
        r"""
        Add a PID to watch.  Alias for `track`.
        """
    def remove_watch(self, pid: builtins.int) -> None:
        r"""
        Remove a PID from watching.  Alias for `untrack`.
        """
    def watch_count(self) -> builtins.int:
        r"""
        Number of currently watched PIDs.  Alias for `tracked_count`.
        """
    def is_watched(self, pid: builtins.int) -> builtins.bool:
        r"""
        Return `True` if the given PID is currently being watched.
        """
    def start(self) -> None:
        r"""
        Start the background watch loop.  No-op if already running.
        """
    def stop(self) -> None:
        r"""
        Stop the background watch loop.  No-op if not running.
        """
    def poll_events(self) -> builtins.list[dict]:
        r"""
        Drain and return all pending events as a list of dicts.
        """
    def is_running(self) -> builtins.bool:
        r"""
        Returns `True` if the background loop is running.
        """
    def tracked_count(self) -> builtins.int:
        r"""
        Number of currently tracked PIDs.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PyPumpedDispatcher:
    r"""
    Thread-affinity aware dispatcher with cooperative main-thread pump.

    Combines an ``ipckit::MainThreadPump`` for ``"main"``/``"named"`` affinity
    jobs with Tokio worker execution for ``"any"`` affinity jobs.

    Call :meth:`pump` from the DCC host's idle callback (e.g. Maya
    ``scriptJob(idleEvent=...)``) to drain pending main-thread work items.

    # Example (Python)

    ```python
    from dcc_mcp_core import PumpedDispatcher

    dispatcher = PumpedDispatcher(budget_ms=8)
    result = dispatcher.submit("update-ui", payload="refresh", affinity="main")
    # In idle callback:
    stats = dispatcher.pump()
    print(f"processed={stats['processed']}, remaining={stats['remaining']}")
    ```
    """
    @property
    def total_dispatched(self) -> builtins.int:
        r"""
        Total items ever dispatched to the main-thread pump.
        """
    @property
    def total_processed(self) -> builtins.int:
        r"""
        Total items ever processed by the main-thread pump.
        """
    @property
    def budget_ms(self) -> builtins.int:
        r"""
        Configured budget in milliseconds.
        """
    def __new__(cls, budget_ms: builtins.int = 8) -> PyPumpedDispatcher:
        r"""
        Create a new pumped dispatcher.

        Parameters
        ----------
        budget_ms : int, optional
            Wall-clock budget per ``pump()`` call in milliseconds (default 8).
        """
    def pump(self) -> dict:
        r"""
        Drain pending main-thread work items.

        Call from the DCC host's idle/update callback. Processes as many
        main-thread jobs as possible within the configured budget.

        Returns a dict with ``processed`` and ``remaining`` counts.
        """
    def pump_with_budget(self, budget_ms: builtins.int) -> dict:
        r"""
        Drain with an explicit budget override for this call only.

        Parameters
        ----------
        budget_ms : int
            Budget in milliseconds for this pump call.
        """
    def submit(self, action_name: builtins.str, payload: typing.Optional[builtins.str] = None, affinity: builtins.str = 'any') -> dict:
        r"""
        Submit a job and block for completion.

        Parameters
        ----------
        action_name : str
            Logical action identifier used as request_id.
        payload : str, optional
            Opaque payload text, returned in the output on success.
        affinity : str, optional
            ``"any"`` (default), ``"main"``, or ``"named:ThreadName"``.
            Main/named jobs are drained by ``pump()``; any jobs run on Tokio.
        """
    def pending(self) -> builtins.int:
        r"""
        Number of main-thread items currently waiting.
        """
    def supported(self) -> builtins.list[builtins.str]:
        r"""
        Return supported affinity values.
        """
    def capabilities(self) -> dict:
        r"""
        Return dispatcher capability flags as a dict.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class PySharedBuffer:
    r"""
    A named, fixed-capacity shared memory buffer backed by an ipckit
    shared memory segment.

    Usage::

        from dcc_mcp_core import PySharedBuffer

        buf = PySharedBuffer.create(capacity=1024 * 1024)  # 1 MiB
        buf.write(b"vertex data")
        data = buf.read()

        # With TTL (auto-expire after 60 seconds)
        buf = PySharedBuffer.create(capacity=1024, ttl_secs=60)
        if buf.is_expired():
            print("buffer has expired")
    """
    ...

@typing.final
class PySharedSceneBuffer:
    r"""
    High-level shared scene buffer for zero-copy DCC ↔ Agent data exchange.

    Usage::

        ssb = PySharedSceneBuffer.write(
            data=vertex_bytes,
            kind=PySceneDataKind.Geometry,
            source_dcc="Maya",
            use_compression=True,
        )
        desc_json = ssb.descriptor_json()
        # Send desc_json to the consumer via IPC…

        # Consumer side:
        recovered = ssb.read()
    """
    ...

@typing.final
class PyStandaloneDispatcher:
    r"""
    Reference host dispatcher implementation for non-DCC environments.

    Supports only ``ThreadAffinity.Any`` and executes submitted jobs on a Tokio
    worker task. Useful for tests and standalone CLI integrations.
    """
    def __new__(cls) -> PyStandaloneDispatcher: ...
    def submit(self, action_name: builtins.str, payload: typing.Optional[builtins.str] = None, affinity: builtins.str = 'any') -> dict:
        r"""
        Submit a lightweight job and block for completion.

        Parameters
        ----------
        action_name : str
            Logical action identifier used as request_id.
        payload : str, optional
            Opaque payload text, returned in the output on success.
        affinity : str, optional
            ``"any"`` (default) or ``"main"``.
            Standalone dispatcher only supports ``"any"``; ``"main"`` will
            return an error outcome.
        """
    def supported(self) -> builtins.list[builtins.str]:
        r"""
        Return supported affinity values.
        """
    def capabilities(self) -> dict:
        r"""
        Return dispatcher capability flags as a dict.
        """

@typing.final
class RateLimitMiddleware:
    r"""
    Rate limiting middleware — limits calls per action per time window.

    Uses a fixed-window counter approach.  Once the limit is reached within
    the window, dispatches return a ``RuntimeError``.

    ```python
    # Allow at most 5 calls per second
    rate_limit = RateLimitMiddleware(max_calls=5, window_ms=1000)
    pipeline.add_rate_limit(max_calls=5, window_ms=1000)
    ```
    """
    @property
    def max_calls(self) -> builtins.int:
        r"""
        Maximum allowed calls per window.
        """
    @property
    def window_ms(self) -> builtins.int:
        r"""
        Window size in milliseconds.
        """
    def __new__(cls, max_calls: builtins.int, window_ms: builtins.int) -> RateLimitMiddleware:
        r"""
        Create a new rate limiter.

        Args:
            max_calls:  Maximum allowed calls per action per ``window_ms``.
            window_ms:  Window size in milliseconds.
        """
    def call_count(self, action: builtins.str) -> builtins.int:
        r"""
        Return the current call count for an action within the current window.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class RecordingGuard:
    r"""
    Guard object returned by `ToolRecorder.start()`.

    Call `finish(success)` to record the result, or let it drop to record as failure.
    """
    def finish(self, success: builtins.bool) -> None:
        r"""
        Finish recording with the given success flag.
        """
    def __enter__(self) -> RecordingGuard:
        r"""
        Context manager support — `__enter__` returns self.
        """
    def __exit__(self, exc_type: typing.Optional[typing.Any], _exc_value: typing.Optional[typing.Any], _traceback: typing.Optional[typing.Any]) -> None:
        r"""
        Context manager support — `__exit__` calls `finish(success=True)`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class RenderOutput:
    r"""
    Python-facing render output metadata.

    ```python
    from dcc_mcp_core import RenderOutput

    out = RenderOutput(file_path="/renders/frame001.png", width=1920, height=1080,
                       format="png", render_time_ms=5000)
    ```
    """
    @property
    def file_path(self) -> builtins.str: ...
    @property
    def width(self) -> builtins.int: ...
    @property
    def height(self) -> builtins.int: ...
    @property
    def format(self) -> builtins.str: ...
    @property
    def render_time_ms(self) -> builtins.int: ...

@typing.final
class ResourceAnnotations:
    r"""
    Annotations for MCP Resource behavior hints.

    Per MCP spec (2025-11-25), resources MAY include annotations that describe
    their audience (user/assistant) and priority level.
    """
    @property
    def audience(self) -> builtins.list[builtins.str]:
        r"""
        Describes who the intended audience is.
        Each element may be "user" or "assistant".
        """
    @audience.setter
    def audience(self, value: typing.Sequence[builtins.str]) -> None:
        r"""
        Describes who the intended audience is.
        Each element may be "user" or "assistant".
        """
    @property
    def priority(self) -> typing.Optional[builtins.float]:
        r"""
        Priority hint for ordering (0.0 = lowest, 1.0 = highest).
        """
    @priority.setter
    def priority(self, value: typing.Optional[builtins.float]) -> None:
        r"""
        Priority hint for ordering (0.0 = lowest, 1.0 = highest).
        """
    def __new__(cls, audience: typing.Sequence[builtins.str] = [], priority: typing.Optional[builtins.float] = None) -> ResourceAnnotations: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ResourceDefinition:
    r"""
    MCP Resource definition.

    Per MCP spec (2025-11-25), a resource has a URI, name, description, MIME type,
    and optional annotations providing audience/priority hints.
    """
    @property
    def uri(self) -> builtins.str: ...
    @uri.setter
    def uri(self, value: builtins.str) -> None: ...
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def description(self) -> builtins.str: ...
    @description.setter
    def description(self, value: builtins.str) -> None: ...
    @property
    def mime_type(self) -> builtins.str: ...
    @mime_type.setter
    def mime_type(self, value: builtins.str) -> None: ...
    @property
    def annotations(self) -> typing.Optional[ResourceAnnotations]:
        r"""
        Optional annotations for this resource.
        """
    @annotations.setter
    def annotations(self, value: typing.Optional[ResourceAnnotations]) -> None:
        r"""
        Optional annotations for this resource.
        """
    def __new__(cls, uri: builtins.str, name: builtins.str, description: builtins.str, mime_type: builtins.str = 'text/plain', annotations: typing.Optional[ResourceAnnotations] = None) -> ResourceDefinition: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ResourceTemplateDefinition:
    r"""
    MCP Resource Template definition.

    Per MCP spec (2025-11-25), a resource template has a URI template, name,
    description, MIME type, and optional annotations.
    """
    @property
    def uri_template(self) -> builtins.str: ...
    @uri_template.setter
    def uri_template(self, value: builtins.str) -> None: ...
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def description(self) -> builtins.str: ...
    @description.setter
    def description(self, value: builtins.str) -> None: ...
    @property
    def mime_type(self) -> builtins.str: ...
    @mime_type.setter
    def mime_type(self, value: builtins.str) -> None: ...
    @property
    def annotations(self) -> typing.Optional[ResourceAnnotations]:
        r"""
        Optional annotations for this resource template.
        """
    @annotations.setter
    def annotations(self, value: typing.Optional[ResourceAnnotations]) -> None:
        r"""
        Optional annotations for this resource template.
        """
    def __new__(cls, uri_template: builtins.str, name: builtins.str, description: builtins.str, mime_type: builtins.str = 'text/plain', annotations: typing.Optional[ResourceAnnotations] = None) -> ResourceTemplateDefinition: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class SandboxContext:
    r"""
    Python-accessible sandbox execution context.

    Example::

        policy = PySandboxPolicy()
        policy.allow_actions(["echo"])
        ctx = PySandboxContext(policy)
        ctx.set_actor("my-agent")

        # Execute with params as a JSON string
        result_json = ctx.execute_json("echo", '{"x": 1}')
    """
    @property
    def action_count(self) -> builtins.int:
        r"""
        Return the number of actions executed in this session.
        """
    @property
    def audit_log(self) -> AuditLog:
        r"""
        Return the :class:`AuditLog` for this context.
        """
    def __new__(cls, policy: SandboxPolicy) -> SandboxContext: ...
    def set_actor(self, actor: builtins.str) -> None:
        r"""
        Set the actor identity attached to audit entries.
        """
    def execute_json(self, action: builtins.str, params_json: builtins.str) -> builtins.str:
        r"""
        Execute an action with parameters provided as a JSON string.

        Returns the handler result as a JSON string, or raises RuntimeError.
        """
    def execute_with_handler(self, action: builtins.str, params_json: builtins.str, handler: typing.Any) -> typing.Any:
        r"""
        Execute an action through the full sandbox pipeline with a Python handler.

        *handler* receives the validated parameter dict and must return a
        JSON-serialisable object. Policy denials and validation failures raise
        :class:`RuntimeError` with the sandbox error message (issue #1001).
        """
    def is_allowed(self, action: builtins.str) -> builtins.bool:
        r"""
        Return ``True`` if ``action`` is permitted by policy.
        """
    def is_path_allowed(self, path: builtins.str) -> builtins.bool:
        r"""
        Return ``True`` if ``path`` is within an allowed directory.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class SandboxPolicy:
    r"""
    Python-accessible sandbox policy builder.

    Example::

        policy = PySandboxPolicy()
        policy.allow_actions(["get_scene_info", "list_objects"])
        policy.deny_actions(["delete_scene"])
        policy.set_timeout_ms(5000)
        policy.set_read_only(True)
    """
    @property
    def is_read_only(self) -> builtins.bool:
        r"""
        Return ``True`` if the policy is in read-only mode.
        """
    def __new__(cls) -> SandboxPolicy: ...
    def allow_actions(self, actions: typing.Sequence[builtins.str]) -> None:
        r"""
        Restrict to only these actions (replaces any previous whitelist).
        """
    def deny_actions(self, actions: typing.Sequence[builtins.str]) -> None:
        r"""
        Always deny these actions.
        """
    def allow_paths(self, paths: typing.Sequence[builtins.str]) -> None:
        r"""
        Allow file-system access under these directory paths.
        """
    def set_timeout_ms(self, ms: builtins.int) -> None:
        r"""
        Set execution timeout (milliseconds).
        """
    def set_max_actions(self, count: builtins.int) -> None:
        r"""
        Set maximum number of actions per session.
        """
    def set_read_only(self, read_only: builtins.bool) -> None:
        r"""
        Enable or disable read-only mode.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class SceneInfo:
    r"""
    Python-facing scene information.
    """
    @property
    def file_path(self) -> builtins.str: ...
    @property
    def name(self) -> builtins.str: ...
    @property
    def modified(self) -> builtins.bool: ...
    @property
    def format(self) -> builtins.str: ...
    @property
    def frame_range(self) -> typing.Optional[tuple[builtins.float, builtins.float]]: ...
    @property
    def current_frame(self) -> typing.Optional[builtins.float]: ...
    @property
    def fps(self) -> typing.Optional[builtins.float]: ...
    @property
    def up_axis(self) -> typing.Optional[builtins.str]: ...
    @property
    def units(self) -> typing.Optional[builtins.str]: ...
    @property
    def statistics(self) -> SceneStatistics: ...
    @property
    def metadata(self) -> builtins.dict[builtins.str, builtins.str]: ...
    def __new__(cls, file_path: builtins.str = '', name: builtins.str = 'untitled', modified: builtins.bool = False, format: builtins.str = '', frame_range: typing.Optional[tuple[builtins.float, builtins.float]] = None, current_frame: typing.Optional[builtins.float] = None, fps: typing.Optional[builtins.float] = None, up_axis: typing.Optional[builtins.str] = None, units: typing.Optional[builtins.str] = None, statistics: typing.Optional[SceneStatistics] = None, metadata: typing.Optional[typing.Mapping[builtins.str, builtins.str]] = None) -> SceneInfo: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class SceneNode:
    r"""
    Python-facing scene hierarchy node (recursive tree).

    ```python
    from dcc_mcp_core import SceneNode, SceneObject

    leaf = SceneNode(
        object=SceneObject(name="pSphere1", object_type="mesh"),
        children=[]
    )
    root = SceneNode(
        object=SceneObject(name="group1", object_type="transform"),
        children=[leaf]
    )
    print(len(root.children))  # 1
    ```
    """
    @property
    def object(self) -> SceneObject:
        r"""
        The scene object at this node.
        """
    @property
    def children(self) -> builtins.list[SceneNode]:
        r"""
        Immediate children of this node.
        """

@typing.final
class SceneObject:
    r"""
    Python-facing lightweight scene object descriptor.

    ```python
    from dcc_mcp_core import SceneObject

    obj = SceneObject(name="pCube1", long_name="|group1|pCube1", object_type="mesh")
    print(obj.name)  # "pCube1"
    ```
    """
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def long_name(self) -> builtins.str: ...
    @long_name.setter
    def long_name(self, value: builtins.str) -> None: ...
    @property
    def object_type(self) -> builtins.str: ...
    @object_type.setter
    def object_type(self, value: builtins.str) -> None: ...
    @property
    def parent(self) -> typing.Optional[builtins.str]: ...
    @parent.setter
    def parent(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def visible(self) -> builtins.bool: ...
    @visible.setter
    def visible(self, value: builtins.bool) -> None: ...
    @property
    def metadata(self) -> builtins.dict[builtins.str, builtins.str]: ...
    @metadata.setter
    def metadata(self, value: typing.Mapping[builtins.str, builtins.str]) -> None: ...

@typing.final
class SceneStatistics:
    r"""
    Python-facing scene statistics.
    """
    @property
    def object_count(self) -> builtins.int: ...
    @object_count.setter
    def object_count(self, value: builtins.int) -> None: ...
    @property
    def vertex_count(self) -> builtins.int: ...
    @vertex_count.setter
    def vertex_count(self, value: builtins.int) -> None: ...
    @property
    def polygon_count(self) -> builtins.int: ...
    @polygon_count.setter
    def polygon_count(self, value: builtins.int) -> None: ...
    @property
    def material_count(self) -> builtins.int: ...
    @material_count.setter
    def material_count(self, value: builtins.int) -> None: ...
    @property
    def texture_count(self) -> builtins.int: ...
    @texture_count.setter
    def texture_count(self, value: builtins.int) -> None: ...
    @property
    def light_count(self) -> builtins.int: ...
    @light_count.setter
    def light_count(self, value: builtins.int) -> None: ...
    @property
    def camera_count(self) -> builtins.int: ...
    @camera_count.setter
    def camera_count(self, value: builtins.int) -> None: ...
    def __new__(cls, object_count: builtins.int = 0, vertex_count: builtins.int = 0, polygon_count: builtins.int = 0, material_count: builtins.int = 0, texture_count: builtins.int = 0, light_count: builtins.int = 0, camera_count: builtins.int = 0) -> SceneStatistics: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ScriptResult:
    r"""
    Python-facing script execution result.
    """
    @property
    def success(self) -> builtins.bool: ...
    @property
    def output(self) -> typing.Optional[builtins.str]: ...
    @property
    def error(self) -> typing.Optional[builtins.str]: ...
    @property
    def execution_time_ms(self) -> builtins.int: ...
    @property
    def context(self) -> builtins.dict[builtins.str, builtins.str]: ...

@typing.final
class SdfPath:
    r"""
    A USD scene description path (e.g. ``/World/Cube``).
    """
    @property
    def is_absolute(self) -> builtins.bool:
        r"""
        Whether this is an absolute path.
        """
    @property
    def name(self) -> builtins.str:
        r"""
        Last path element name (e.g. ``"Cube"`` for ``/World/Cube``).
        """
    def __new__(cls, path: builtins.str) -> SdfPath: ...
    def __str__(self) -> builtins.str:
        r"""
        String representation of this path.
        """
    def __repr__(self) -> builtins.str: ...
    def __eq__(self, other: SdfPath) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...
    def child(self, name: builtins.str) -> SdfPath:
        r"""
        Append a child segment and return a new path.
        """
    def parent(self) -> typing.Optional[SdfPath]:
        r"""
        Parent path, or ``None`` for the root path.
        """

@typing.final
class SemVer:
    r"""
    A semantic version consisting of major, minor, and patch components.

    Only the numeric components are considered; pre-release labels (e.g. `-alpha`)
    are stripped and ignored for comparison purposes.
    """
    @property
    def major(self) -> builtins.int: ...
    @property
    def minor(self) -> builtins.int: ...
    @property
    def patch(self) -> builtins.int: ...
    def __new__(cls, major: builtins.int, minor: builtins.int = 0, patch: builtins.int = 0) -> SemVer: ...
    @staticmethod
    def parse(s: builtins.str) -> SemVer:
        r"""
        Parse a semver string such as "1.2.3", "v2.0", or "1.0.0-alpha".

        Raises ValueError if the string cannot be parsed.
        """
    def matches_constraint(self, constraint: VersionConstraint) -> builtins.bool:
        r"""
        Check whether this version satisfies a constraint string.

        Equivalent to ``VersionConstraint.parse(constraint_str).matches(self)``.
        """
    def __str__(self) -> builtins.str: ...
    def __repr__(self) -> builtins.str: ...
    def __eq__(self, other: SemVer) -> builtins.bool: ...
    def __lt__(self, other: SemVer) -> builtins.bool: ...
    def __le__(self, other: SemVer) -> builtins.bool: ...
    def __gt__(self, other: SemVer) -> builtins.bool: ...
    def __ge__(self, other: SemVer) -> builtins.bool: ...

@typing.final
class ServiceEntry:
    r"""
    Python-facing service entry representing a discovered DCC instance.

    ```python,ignore
    from dcc_mcp_core import TransportManager

    transport = TransportManager("/path/to/registry")
    instance_id = transport.register_service("maya", "127.0.0.1", 18812)
    entry = transport.get_service("maya", instance_id)
    print(entry.dcc_type)      # "maya"
    print(entry.host)          # "127.0.0.1"
    print(entry.port)          # 18812
    print(entry.status)        # ServiceStatus.AVAILABLE
    ```
    """
    @property
    def schema_version(self) -> builtins.int:
        r"""
        Version of the serialized service-registry schema.
        """
    @property
    def dcc_type(self) -> builtins.str:
        r"""
        DCC application type (e.g. "maya", "houdini", "blender").
        """
    @property
    def instance_id(self) -> builtins.str:
        r"""
        Unique instance identifier (UUID string).
        """
    @property
    def host(self) -> builtins.str:
        r"""
        Host address.
        """
    @property
    def port(self) -> builtins.int:
        r"""
        Port number.
        """
    @property
    def version(self) -> typing.Optional[builtins.str]:
        r"""
        DCC application version (e.g. "2024.2").
        """
    @property
    def scene(self) -> typing.Optional[builtins.str]:
        r"""
        Currently active scene / document.

        For single-document DCCs this is the open file path.
        For multi-document apps (Photoshop) this is the **focused** document.
        """
    @property
    def documents(self) -> builtins.list[builtins.str]:
        r"""
        All documents currently open in this instance.

        Empty for DCCs that only support one document at a time.
        For multi-document apps each element is a file path.
        """
    @property
    def pid(self) -> typing.Optional[builtins.int]:
        r"""
        OS process ID of the registered service owner.

        For sidecars this differs from `host_pid`, which identifies the DCC.
        """
    @property
    def host_pid(self) -> typing.Optional[builtins.int]:
        r"""
        Optional PID of an external DCC host that bounds this service lifetime.
        """
    @property
    def display_name(self) -> typing.Optional[builtins.str]:
        r"""
        Human-readable label for this instance (e.g. `"Maya-Rigging"`).

        Set by the bridge plugin at registration time.  Displayed by the
        agent when asking the user to choose between multiple instances.
        """
    @property
    def metadata(self) -> builtins.dict[builtins.str, builtins.str]:
        r"""
        Arbitrary metadata.
        """
    @property
    def status(self) -> ServiceStatus:
        r"""
        Current service status.
        """
    @property
    def transport_address(self) -> typing.Optional[TransportAddress]:
        r"""
        Transport address (None = TCP host:port).
        """
    @property
    def last_heartbeat_ms(self) -> builtins.int:
        r"""
        Last heartbeat timestamp in milliseconds since Unix epoch.

        Useful for `LazySessionPool` implementations to determine if a session
        has been idle too long and should be evicted.  Updated by
        :meth:`TransportManager.heartbeat`.
        """

@typing.final
class SkillBranding:
    r"""
    Author-supplied visual identity for a skill's marketplace card.
    """
    @property
    def accent_color(self) -> typing.Optional[builtins.str]:
        r"""
        CSS-friendly primary accent (e.g. `"#ff7a45"`, `"hsl(28 90% 56%)"`).
        Falls back to a hash-derived hue when omitted.
        """
    @property
    def secondary_color(self) -> typing.Optional[builtins.str]:
        r"""
        Secondary accent for gradient backgrounds. Optional.
        """
    @property
    def emoji(self) -> typing.Optional[builtins.str]:
        r"""
        Short brand glyph — typically an emoji or unicode mark. Cards
        prefer this over a DCC icon when present.
        """
    @property
    def logo_url(self) -> typing.Optional[builtins.str]:
        r"""
        HTTPS URL to a square SVG or PNG logo. When provided the card
        shows the logo in place of both `emoji` and the DCC icon.
        """
    @property
    def tagline(self) -> typing.Optional[builtins.str]:
        r"""
        One-line tagline shown under the skill name. Plain text, no markdown.
        """

@typing.final
class SkillCatalog:
    r"""
    Manages discovered skills and their progressive loading.

    Thread-safe: all state is stored in `DashMap` / `DashSet`.

    When a dispatcher is attached (via [`SkillCatalog::with_dispatcher`]),
    loading a skill also registers a handler for each action — enabling the
    Skills-First workflow where agents never need to register handlers manually.

    # Execution modes

    - **In-process** (preferred inside a DCC): register a
      [`ScriptExecutorFn`] via [`with_in_process_executor`](Self::with_in_process_executor).
      Scripts are executed directly in the host DCC's Python interpreter so
      DCC APIs (`maya.cmds`, `bpy`, `hou`, …) are available without spawning
      any external process.
    - **Subprocess** (default): each skill script is executed as a child
      process. Suitable for standalone / non-DCC environments.
    """
    def __new__(cls, registry: ToolRegistry) -> SkillCatalog: ...
    def event_bus(self) -> EventBus:
        r"""
        Return the catalog lifecycle event bus.
        """
    def activate_group(self, group_name: builtins.str) -> builtins.int:
        r"""
        Activate a tool group (enable every action in it).

        Returns the number of actions whose ``enabled`` flag changed.
        """
    def deactivate_group(self, group_name: builtins.str) -> builtins.int:
        r"""
        Deactivate a tool group.
        """
    def active_groups(self) -> builtins.list[builtins.str]:
        r"""
        Return the list of currently-active tool groups.
        """
    def list_groups(self) -> builtins.list[tuple[builtins.str, builtins.str, builtins.bool]]:
        r"""
        List all declared groups as ``(skill_name, group_name, active)`` tuples.
        """
    def discover(self, extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> builtins.int:
        r"""
        Discover skills from standard scan paths.

        Args:
            extra_paths: Additional directories to scan.
            dcc_name: DCC name filter (e.g. "maya", "blender").

        Returns the number of newly discovered skills.
        """
    def rediscover(self, extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> builtins.int:
        r"""
        Re-scan search paths, replace changed skill metadata, and refresh any
        loaded tools whose declarations changed on disk.
        """
    def set_in_process_executor(self, executor: typing.Optional[typing.Any]) -> None:
        r"""
        Register a Python callable as the in-process script executor.

        When registered, skill scripts are executed inside the **current**
        Python interpreter (the one running inside the DCC application) rather
        than being spawned as a subprocess.  This is the correct behaviour for
        Maya, Blender, Houdini, and any other DCC that embeds its own Python —
        the callable receives the script path and a params dict and must return
        a JSON-serialisable dict.

        The callable signature must be:

        ```python
            def executor(script_path: str, params: dict, *, action_name: str,
                         skill_name: str | None, thread_affinity: str,
                         execution: str, timeout_hint_secs: int | None) -> dict:
                ...
        ```

        Example (Maya adapter):

        ```python
            def _maya_exec(script_path: str, params: dict) -> dict:
                import importlib.util, sys
                spec = importlib.util.spec_from_file_location("_skill_script", script_path)
                mod = importlib.util.module_from_spec(spec)
                mod.__mcp_params__ = params
                spec.loader.exec_module(mod)
                return getattr(mod, "__mcp_result__", {"success": True})

            catalog.set_in_process_executor(_maya_exec)
        ```

        Pass ``None`` to revert to subprocess execution.
        """
    def set_skill_load_transform(self, transform: typing.Optional[typing.Any]) -> None:
        r"""
        Register a callable that can mutate or veto skill metadata before load.

        The callable receives a detached ``SkillMetadata`` object. It may mutate
        that object and return ``None``, or return a replacement
        ``SkillMetadata``. Raising an exception vetoes the load. The skill name
        must remain unchanged.
        """
    def clear_skill_load_transform(self) -> None:
        r"""
        Clear any registered skill-load transform.
        """
    def set_after_load_skill_hook(self, hook: typing.Optional[typing.Any]) -> None:
        r"""
        Register a callable that observes successfully registered skill tools.

        The callable receives ``(skill_metadata, registered_actions)``. Errors
        are logged as lifecycle events but do not roll back the already-loaded
        skill; use ``set_skill_load_transform`` when policy must veto a load.
        """
    def clear_after_load_skill_hook(self) -> None:
        r"""
        Clear any registered after-load skill hook.
        """
    def set_after_unload_skill_hook(self, hook: typing.Optional[typing.Any]) -> None:
        r"""
        Register a callable invoked after a skill is unloaded (#1405).

        The callable receives ``(skill_name, unregistered_actions)``. Used
        by the persistence layer to evict the row from the on-disk store.
        """
    def clear_after_unload_skill_hook(self) -> None:
        r"""
        Clear any registered after-unload skill hook.
        """
    def set_after_group_change_hook(self, hook: typing.Optional[typing.Any]) -> None:
        r"""
        Register a callable invoked after a tool group is activated or
        deactivated (#1405). Receives ``(group_name, activated: bool)``.
        """
    def clear_after_group_change_hook(self) -> None:
        r"""
        Clear any registered after-group-change hook.
        """
    def set_after_scoped_group_change_hook(self, hook: typing.Optional[typing.Any]) -> None:
        r"""
        Register the persistence-only observer for exact scoped group keys.
        """
    def clear_after_scoped_group_change_hook(self) -> None:
        r"""
        Clear the scoped persistence observer.
        """
    def replay_loaded(self, state_json: builtins.str, policy: builtins.str = 'skip_on_drift') -> builtins.str:
        r"""
        Replay a persisted set of loaded skills + active groups (#1405).

        ``state_json`` is the JSON-encoded ``PersistedCatalogState`` (the
        shape returned by ``LoadedStateStore.snapshot().to_json()`` plus
        ``json.dumps``).

        ``policy`` is one of ``"skip_on_drift"`` (default),
        ``"require_exact_version"``, or ``"ignore_version"``.

        Returns the ``ReplayReport`` as a JSON-encoded string so callers
        can parse it with ``json.loads`` without an extra Python
        conversion dependency.
        """
    def load_skill(self, skill_name: builtins.str) -> builtins.list[builtins.str]:
        r"""
        Load a skill by name — registers its tools.

        Returns a list of registered action names.
        Raises ValueError if the skill is not found.
        """
    def get_skill(self, skill_name: builtins.str) -> typing.Optional[SkillMetadata]:
        r"""
        Return a detached, mutable skill metadata object.

        Mutating the returned object does not affect catalog state until it is
        passed back to ``load_skill_object``.
        """
    def load_skill_object(self, metadata: SkillMetadata) -> builtins.list[builtins.str]:
        r"""
        Load a caller-supplied skill metadata object through core registration.

        This lets adapters adjust tool declarations at runtime without parsing
        or rewriting ``SKILL.md`` / ``tools.yaml`` sidecar files.
        """
    def unload_skill(self, skill_name: builtins.str) -> builtins.int:
        r"""
        Unload a skill — removes its tools from the registry.

        Returns the number of actions removed.
        Raises ValueError if the skill is not loaded.
        """
    def search_skills(self, query: typing.Optional[builtins.str] = None, tags: typing.Sequence[builtins.str] = [], dcc: typing.Optional[builtins.str] = None, scope: typing.Optional[builtins.str] = None, limit: typing.Optional[builtins.int] = None) -> builtins.list[SkillSummary]:
        r"""
        Unified skill discovery (issue #340).

        Optional ``scope`` (str: "repo" |
        "user" | "system" | "admin") and ``limit``. Empty ``query`` with no
        other filters returns the top ``limit`` skills ordered by scope
        precedence (Admin > System > User > Repo) then name.
        """
    def list_skills(self, status: typing.Optional[builtins.str] = None) -> builtins.list[SkillSummary]:
        r"""
        List all skills with their load status.
        """
    def get_skill_info(self, skill_name: builtins.str) -> typing.Optional[typing.Any]:
        r"""
        Get detailed info about a specific skill.

        Returns None if the skill is not found. The detail is returned as a
        Python dict (serialized via serde_json).
        """
    def __len__(self) -> builtins.int:
        r"""
        Number of skills in the catalog.
        """
    def __bool__(self) -> builtins.bool:
        r"""
        Whether the catalog is empty.
        """
    def is_loaded(self, skill_name: builtins.str) -> builtins.bool:
        r"""
        Check if a skill is loaded.
        """
    def loaded_count(self) -> builtins.int:
        r"""
        Number of loaded skills.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class SkillGroup:
    r"""
    Declaration of a tool group within a skill (progressive exposure).

    A group bundles multiple tools behind a single stub entry in ``tools/list``
    so agents only pay the context cost for the tools they actually use.

    ```yaml
    groups:
      - name: uv-editing
        description: UV-space operations
        default-active: false
        tools: [unwrap, layout_uvs, transfer_uvs]
    ```
    """
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, name: builtins.str, description: builtins.str = '', tools: typing.Sequence[builtins.str] = [], default_active: builtins.bool = False) -> SkillGroup: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class SkillLinks:
    r"""
    Author-supplied external references rendered as a chip row on the card.
    """
    @property
    def docs(self) -> typing.Optional[builtins.str]:
        r"""
        Documentation entry point (most prominent link).
        """
    @property
    def repo(self) -> typing.Optional[builtins.str]:
        r"""
        Public source repository URL.
        """
    @property
    def homepage(self) -> typing.Optional[builtins.str]:
        r"""
        Marketing / project homepage.
        """
    @property
    def issues(self) -> typing.Optional[builtins.str]:
        r"""
        Issue tracker URL — surfaces a quick-feedback chip on the card.
        """
    @property
    def chat(self) -> typing.Optional[builtins.str]:
        r"""
        Public chat / support channel — Discord, Slack invite, etc.
        """

@typing.final
class SkillMetadata:
    r"""
    Metadata parsed from a SKILL.md frontmatter.

    Supports all three skill standards:

    ## Minimal (agentskills.io compatible)
    ```yaml
    ---
    name: my-skill
    description: What it does and when to use it.
    ---
    ```

    ## Full (all standards)
    ```yaml
    ---
    name: maya-bevel
    description: Bevel tools for Maya polygon modeling.
    # agentskills.io standard
    license: MIT
    compatibility: Maya 2022+, Python 3.7+
    allowed-tools: Bash Read
    metadata:
      author: studio-name
      category: modeling
    # ClawHub / OpenClaw
    version: "1.0.0"
    metadata:
      openclaw:
        requires:
          bins: [maya]
        emoji: "🎨"
        homepage: https://example.com
    # dcc-mcp-core extensions
    dcc: maya
    tags: [modeling, polygon]
    tools:
      - name: bevel
        description: Apply bevel to selected edges
    ---
    ```
    """
    ...

@typing.final
class SkillRuntimeDescriptor:
    r"""
    A single optional runtime descriptor from `metadata.dcc-mcp.runtimes`.
    """
    @property
    def name(self) -> builtins.str:
        r"""
        Stable runtime identifier, for example `usd-core` or `usdcat`.
        """
    @property
    def kind(self) -> SkillRuntimeKind:
        r"""
        Runtime category.
        """
    @property
    def description(self) -> typing.Optional[builtins.str]:
        r"""
        Human-readable explanation of what this runtime unlocks.
        """
    @property
    def feature_level(self) -> typing.Optional[builtins.str]:
        r"""
        Optional feature level exposed when this runtime is available.
        """
    @property
    def package(self) -> typing.Optional[builtins.str]:
        r"""
        Python package name, such as `usd-core`.
        """
    @property
    def module(self) -> typing.Optional[builtins.str]:
        r"""
        Python module name to check with `importlib.util.find_spec`.
        """
    @property
    def extra(self) -> typing.Optional[builtins.str]:
        r"""
        Python extra name, such as `usd-core`.
        """
    @property
    def binary(self) -> typing.Optional[builtins.str]:
        r"""
        Binary name to resolve on `PATH`.
        """
    @property
    def env(self) -> typing.Optional[builtins.str]:
        r"""
        Environment variable name to check.
        """
    @property
    def optional(self) -> builtins.bool:
        r"""
        Whether the skill can still run when this runtime is absent.
        """
    @property
    def guidance(self) -> typing.Optional[builtins.str]:
        r"""
        Human-actionable install or configuration guidance.
        """

@typing.final
class SkillRuntimeReport:
    r"""
    Resolved runtime row exposed by search/list/describe surfaces.
    """
    @property
    def name(self) -> builtins.str: ...
    @property
    def kind(self) -> SkillRuntimeKind: ...
    @property
    def state(self) -> SkillRuntimeState: ...
    @property
    def description(self) -> typing.Optional[builtins.str]: ...
    @property
    def feature_level(self) -> typing.Optional[builtins.str]: ...
    @property
    def package(self) -> typing.Optional[builtins.str]: ...
    @property
    def module(self) -> typing.Optional[builtins.str]: ...
    @property
    def extra(self) -> typing.Optional[builtins.str]: ...
    @property
    def binary(self) -> typing.Optional[builtins.str]: ...
    @property
    def env(self) -> typing.Optional[builtins.str]: ...
    @property
    def guidance(self) -> typing.Optional[builtins.str]: ...

@typing.final
class SkillRuntimeSummary:
    r"""
    Aggregated runtime status for compact discovery.
    """
    @property
    def state(self) -> SkillRuntimeState: ...
    @property
    def available(self) -> builtins.int: ...
    @property
    def degraded(self) -> builtins.int: ...
    @property
    def missing(self) -> builtins.int: ...
    @property
    def total(self) -> builtins.int: ...

@typing.final
class SkillScanner:
    r"""
    Scanner for discovering Skill packages in directories.
    """
    @property
    def discovered_skills(self) -> builtins.list[builtins.str]: ...
    def __new__(cls) -> SkillScanner: ...
    def scan(self, extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None, force_refresh: builtins.bool = False) -> builtins.list[builtins.str]: ...
    def clear_cache(self) -> None: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class SkillSummary:
    r"""
    Lightweight summary of a skill for search/list results.
    """
    @property
    def name(self) -> builtins.str: ...
    @property
    def description(self) -> builtins.str: ...
    @property
    def search_hint(self) -> builtins.str:
        r"""
        Short hint used for keyword search (from SKILL.md `search-hint` field).
        Falls back to description if not set in SKILL.md.
        """
    @property
    def tags(self) -> builtins.list[builtins.str]: ...
    @property
    def dcc(self) -> builtins.str: ...
    @property
    def version(self) -> builtins.str: ...
    @property
    def tool_count(self) -> builtins.int: ...
    @property
    def tool_names(self) -> builtins.list[builtins.str]:
        r"""
        RTK-inspired: compact comma-separated format when serialized to reduce token consumption.
        """
    @property
    def loaded(self) -> builtins.bool: ...
    @property
    def status(self) -> builtins.str:
        r"""
        Machine-readable load status: `"discovered"`, `"pending_deps"`,
        `"loaded"`, or `"error"`.
        """
    @property
    def missing_dependencies(self) -> builtins.list[builtins.str]:
        r"""
        Declared dependencies not currently present in the catalog.
        """
    @property
    def scope(self) -> builtins.str:
        r"""
        Trust level / origin scope of this skill (e.g. `"repo"`, `"user"`, `"system"`).
        """
    @property
    def path_source(self) -> builtins.str:
        r"""
        Discovery path source used by the shared ranking policy.
        """
    @property
    def implicit_invocation(self) -> builtins.bool:
        r"""
        `true` when this skill declares `allow_implicit_invocation: false`.
        """
    @property
    def layer(self) -> typing.Optional[builtins.str]:
        r"""
        Architectural layer from `metadata.dcc-mcp.layer`
        (`"infrastructure"` / `"domain"` / `"example"`).
        `None` when the skill does not declare one.
        """
    @property
    def stage(self) -> typing.Optional[builtins.str]:
        r"""
        Pipeline stage from `metadata.dcc-mcp.stage`. Free-form, owned by
        each DCC adapter's vocabulary. `None` when the skill does not
        declare one.

        Surfacing this on the summary lets adapters compute "skills in
        stage X" queries (and minimal-mode presets) directly from
        `list_skills()` / `search_skills()` without having to round-trip
        through `get_skill_info()` or maintain an out-of-band hard-coded
        shadow table.
        """
    @property
    def runtime(self) -> typing.Optional[SkillRuntimeSummary]:
        r"""
        Aggregated optional runtime state from `metadata.dcc-mcp.runtimes`.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class SkillWatcher:
    r"""
    Python-facing wrapper for [`SkillWatcher`].
    """
    def __new__(cls, debounce_ms: builtins.int = 300) -> SkillWatcher:
        r"""
        Create a new SkillWatcher.

        Args:
            debounce_ms: Milliseconds to wait before reloading after a change
                         (default: 300).
        """
    def watch(self, path: builtins.str) -> None:
        r"""
        Start watching *path* for skill changes.

        An immediate reload is performed so skills are available without waiting
        for a filesystem event.

        Raises:
            RuntimeError: If the path cannot be watched.
        """
    def unwatch(self, path: builtins.str) -> builtins.bool:
        r"""
        Stop watching *path*.

        Returns ``True`` if the path was being watched, ``False`` otherwise.
        """
    def skills(self) -> builtins.list[SkillMetadata]:
        r"""
        Return the current skill snapshot as a list.
        """
    def skill_count(self) -> builtins.int:
        r"""
        Return the number of loaded skills.
        """
    def watched_paths(self) -> builtins.list[builtins.str]:
        r"""
        Return the list of watched directory paths.
        """
    def reload(self) -> None:
        r"""
        Manually trigger a reload.
        """
    def on_reload(self, callback: typing.Any) -> None:
        r"""
        Register a callback invoked after every automatic or manual reload.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class SocketServerAdapter:
    r"""
    Minimal wrapper for ``ipckit::SocketServer``.

    Create a multi-client Unix socket or named-pipe server.
    """
    @property
    def socket_path(self) -> builtins.str:
        r"""
        The socket path this server is listening on.
        """
    @property
    def connection_count(self) -> builtins.int:
        r"""
        Number of currently connected clients.
        """
    def __new__(cls, path: builtins.str, max_connections: builtins.int = 10, connection_timeout_ms: builtins.int = 30000) -> SocketServerAdapter:
        r"""
        Create a new socket server.

        Args:
            path: Socket path (Unix) or pipe name (Windows).
            max_connections: Maximum concurrent connections. Defaults to 10.
            connection_timeout_ms: Connection timeout in ms. Defaults to 30000.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class TelemetryConfig:
    r"""
    Builder and initialiser for the global telemetry provider.

    # Example

    ```python
    from dcc_mcp_core import TelemetryConfig

    cfg = (TelemetryConfig("my-service")
            .with_stdout_exporter()
            .with_attribute("dcc.name", "maya"))
    cfg.init()   # install global provider
    cfg.shutdown()  # flush and close
    ```
    """
    @property
    def service_name(self) -> builtins.str:
        r"""
        Return the service name.
        """
    @property
    def enable_metrics(self) -> builtins.bool:
        r"""
        Return whether metrics are enabled.
        """
    @property
    def enable_tracing(self) -> builtins.bool:
        r"""
        Return whether tracing is enabled.
        """
    def __new__(cls, service_name: builtins.str) -> TelemetryConfig:
        r"""
        Create a new config for the given service name.
        """
    def with_stdout_exporter(self) -> TelemetryConfig:
        r"""
        Use the stdout exporter (prints spans/metrics to stdout).
        """
    def with_noop_exporter(self) -> TelemetryConfig:
        r"""
        Use the no-op exporter (discard all telemetry — useful in tests).
        """
    def with_otlp_exporter(self, endpoint: builtins.str) -> TelemetryConfig:
        r"""
        Use the OTLP gRPC exporter (requires the `otlp-exporter` Cargo feature).

        The endpoint defaults to `http://localhost:4317` if not set here.
        The `OTEL_EXPORTER_OTLP_ENDPOINT` environment variable always overrides
        the value supplied to this method.
        """
    def with_json_logs(self) -> TelemetryConfig:
        r"""
        Use JSON log format.
        """
    def with_text_logs(self) -> TelemetryConfig:
        r"""
        Use text log format (default).
        """
    def with_attribute(self, key: builtins.str, value: builtins.str) -> TelemetryConfig:
        r"""
        Add an extra resource attribute.
        """
    def with_service_version(self, version: builtins.str) -> TelemetryConfig:
        r"""
        Set the service version string.
        """
    def set_enable_metrics(self, enabled: builtins.bool) -> TelemetryConfig:
        r"""
        Enable or disable metrics collection.
        """
    def set_enable_tracing(self, enabled: builtins.bool) -> TelemetryConfig:
        r"""
        Enable or disable distributed tracing.
        """
    def init(self) -> None:
        r"""
        Install this configuration as the global telemetry provider.

        Raises `RuntimeError` if a provider is already installed.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class TimingMiddleware:
    r"""
    Timing middleware — measures per-action latency.

    After each dispatch, elapsed time is available via :meth:`last_elapsed_ms`.

    ```python
    timing = TimingMiddleware()
    pipeline.add_timing()
    pipeline.dispatch("my_action", "{}")
    print(timing.last_elapsed_ms("my_action"))  # milliseconds
    ```
    """
    def __new__(cls) -> TimingMiddleware: ...
    def last_elapsed_ms(self, action: builtins.str) -> typing.Optional[builtins.int]:
        r"""
        Return the last recorded elapsed time for an action in milliseconds.

        Returns None if the action has not been dispatched yet.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolAnnotations:
    r"""
    Annotations for MCP Tool behavior hints.

    Per MCP spec (2025-11-25), tools MAY include annotations that describe
    their destructive/idempotent nature and open-world safety.

    This is the **canonical** wire-format type for tool annotations. The
    `dcc-mcp-jsonrpc` crate re-exports this under the historical
    `McpToolAnnotations` name (#812 part 1). The `dcc-mcp-models` crate
    owns the distinct `SkillToolAnnotations` source-metadata projection with
    snake_case + authoring aliases. Project it explicitly when crossing into
    this camelCase wire layer.
    """
    @property
    def title(self) -> typing.Optional[builtins.str]: ...
    @title.setter
    def title(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def read_only_hint(self) -> typing.Optional[builtins.bool]: ...
    @read_only_hint.setter
    def read_only_hint(self, value: typing.Optional[builtins.bool]) -> None: ...
    @property
    def destructive_hint(self) -> typing.Optional[builtins.bool]: ...
    @destructive_hint.setter
    def destructive_hint(self, value: typing.Optional[builtins.bool]) -> None: ...
    @property
    def idempotent_hint(self) -> typing.Optional[builtins.bool]: ...
    @idempotent_hint.setter
    def idempotent_hint(self, value: typing.Optional[builtins.bool]) -> None: ...
    @property
    def open_world_hint(self) -> typing.Optional[builtins.bool]: ...
    @open_world_hint.setter
    def open_world_hint(self, value: typing.Optional[builtins.bool]) -> None: ...
    @property
    def deferred_hint(self) -> typing.Optional[builtins.bool]: ...
    @deferred_hint.setter
    def deferred_hint(self, value: typing.Optional[builtins.bool]) -> None: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, title: typing.Optional[builtins.str] = None, read_only_hint: typing.Optional[builtins.bool] = None, destructive_hint: typing.Optional[builtins.bool] = None, idempotent_hint: typing.Optional[builtins.bool] = None, open_world_hint: typing.Optional[builtins.bool] = None, deferred_hint: typing.Optional[builtins.bool] = None) -> ToolAnnotations: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolDeclaration:
    r"""
    Declaration of a tool provided by a skill, parsed from SKILL.md frontmatter.

    Unlike `ToolMeta`, this is a lightweight declaration that can be discovered
    without loading the skill's scripts. It carries enough information for agents
    to decide whether to load a skill.
    """
    @property
    def requires_in_process(self) -> builtins.bool: ...
    @requires_in_process.setter
    def requires_in_process(self, value: builtins.bool) -> None: ...
    @property
    def execution(self) -> builtins.str: ...
    @execution.setter
    def execution(self, value: builtins.str) -> None: ...
    @property
    def job_strategy(self) -> builtins.str: ...
    @job_strategy.setter
    def job_strategy(self, value: builtins.str) -> None: ...
    @property
    def input_schema(self) -> builtins.str: ...
    @input_schema.setter
    def input_schema(self, value: builtins.str) -> None: ...
    @property
    def output_schema(self) -> builtins.str: ...
    @output_schema.setter
    def output_schema(self, value: builtins.str) -> None: ...
    @property
    def annotations(self) -> typing.Any: ...
    @annotations.setter
    def annotations(self, value: typing.Optional[typing.Any]) -> None: ...
    @property
    def next_tools(self) -> typing.Optional[dict]: ...
    @next_tools.setter
    def next_tools(self, value: typing.Optional[typing.Any]) -> None: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, name: builtins.str, description: builtins.str = '', input_schema: typing.Optional[builtins.str] = None, output_schema: typing.Optional[builtins.str] = None, read_only: builtins.bool = False, destructive: builtins.bool = False, idempotent: builtins.bool = False, defer_loading: builtins.bool = False, source_file: builtins.str = '', group: builtins.str = '', execution: builtins.str = 'sync', timeout_hint_secs: typing.Optional[builtins.int] = None, job_strategy: builtins.str = 'monolithic', thread_affinity: builtins.str = 'any', enforce_thread_affinity: builtins.bool = False, search_aliases: typing.Sequence[builtins.str] = [], requires_in_process: builtins.bool = False) -> ToolDeclaration: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolDefinition:
    r"""
    MCP Tool definition schema.

    Per MCP spec (2025-11-25), a tool has a name, description, input/output schemas,
    and optional annotations providing behavioral hints.
    """
    @property
    def name(self) -> builtins.str: ...
    @name.setter
    def name(self, value: builtins.str) -> None: ...
    @property
    def description(self) -> builtins.str: ...
    @description.setter
    def description(self, value: builtins.str) -> None: ...
    @property
    def input_schema(self) -> builtins.str: ...
    @input_schema.setter
    def input_schema(self, value: builtins.str) -> None: ...
    @property
    def output_schema(self) -> typing.Optional[builtins.str]: ...
    @output_schema.setter
    def output_schema(self, value: typing.Optional[builtins.str]) -> None: ...
    @property
    def annotations(self) -> typing.Optional[ToolAnnotations]:
        r"""
        Optional behavioral annotations for this tool.
        """
    @annotations.setter
    def annotations(self, value: typing.Optional[ToolAnnotations]) -> None:
        r"""
        Optional behavioral annotations for this tool.
        """
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, name: builtins.str, description: builtins.str, input_schema: builtins.str, output_schema: typing.Optional[builtins.str] = None, annotations: typing.Optional[ToolAnnotations] = None) -> ToolDefinition: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolDispatcher:
    r"""
    Routes action calls to registered Python callables with automatic validation.

    Example::

        import json
        from dcc_mcp_core import ToolRegistry, ToolDispatcher

        reg = ToolRegistry()
        reg.register(
            "create_sphere",
            input_schema=json.dumps({
                "type": "object",
                "required": ["radius"],
                "properties": {"radius": {"type": "number", "minimum": 0.0}},
            }),
        )
        dispatcher = ToolDispatcher(reg)

        def create_sphere(params):
            return {"created": True, "radius": params["radius"]}

        dispatcher.register_handler("create_sphere", create_sphere)
        result = dispatcher.dispatch("create_sphere", json.dumps({"radius": 2.0}))
        assert result["output"]["created"] is True
    """
    @property
    def skip_empty_schema_validation(self) -> builtins.bool:
        r"""
        Whether to skip validation when the action schema is empty.
        """
    @skip_empty_schema_validation.setter
    def skip_empty_schema_validation(self, value: builtins.bool) -> None: ...
    def __new__(cls, registry: ToolRegistry) -> ToolDispatcher:
        r"""
        Create a new dispatcher backed by the given registry.
        """
    def event_bus(self) -> EventBus:
        r"""
        Return the dispatcher event bus.
        """
    def set_event_bus(self, event_bus: EventBus) -> None:
        r"""
        Replace the dispatcher event bus.
        """
    def register_handler(self, action_name: builtins.str, handler: typing.Any) -> None:
        r"""
        Register a Python callable as the handler for ``action_name``.

        Raises:
            TypeError: If ``handler`` is not callable.
        """
    def remove_handler(self, action_name: builtins.str) -> builtins.bool:
        r"""
        Remove the handler for ``action_name``.

        Returns ``True`` if a handler existed and was removed.
        """
    def has_handler(self, action_name: builtins.str) -> builtins.bool:
        r"""
        Return ``True`` if a handler is registered for ``action_name``.
        """
    def handler_count(self) -> builtins.int:
        r"""
        Number of registered handlers.
        """
    def handler_names(self) -> builtins.list[builtins.str]:
        r"""
        Alphabetically sorted list of registered handler names.
        """
    def dispatch(self, action_name: builtins.str, params_json: builtins.str = 'null', meta_json: builtins.str = 'null') -> dict:
        r"""
        Dispatch an action call.

        1. Validates ``params_json`` against the action schema.
        2. Calls the registered Python handler.
        3. Returns a dict with ``"action"``, ``"output"``, ``"validation_skipped"``.

        Raises:
            KeyError:     No handler registered for ``action_name``.
            ValueError:   Invalid JSON or validation failure.
            RuntimeError: Handler raised an exception.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolMetrics:
    r"""
    Read-only snapshot of per-Action performance metrics.
    """
    @property
    def action_name(self) -> builtins.str:
        r"""
        Action name.
        """
    @property
    def invocation_count(self) -> builtins.int:
        r"""
        Total number of invocations.
        """
    @property
    def success_count(self) -> builtins.int:
        r"""
        Number of successful invocations.
        """
    @property
    def failure_count(self) -> builtins.int:
        r"""
        Number of failed invocations.
        """
    @property
    def avg_duration_ms(self) -> builtins.float:
        r"""
        Average execution duration in milliseconds.
        """
    @property
    def p95_duration_ms(self) -> builtins.float:
        r"""
        P95 execution duration in milliseconds.
        """
    @property
    def p99_duration_ms(self) -> builtins.float:
        r"""
        P99 execution duration in milliseconds.
        """
    def success_rate(self) -> builtins.float:
        r"""
        Success rate as a fraction in [0.0, 1.0].
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolPipeline:
    r"""
    Middleware-wrapped ToolDispatcher.

    Allows attaching cross-cutting concerns (logging, timing, audit, rate
    limiting) to action dispatch without modifying individual action handlers.

    Middleware runs in registration order for ``before_dispatch``, and in
    **reverse** order for ``after_dispatch`` (standard onion model).

    ```python
    import json
    from dcc_mcp_core import ToolRegistry, ToolDispatcher, ToolPipeline

    reg = ToolRegistry()
    reg.register("ping", category="util")

    dispatcher = ToolDispatcher(reg)
    dispatcher.register_handler("ping", lambda params: "pong")

    pipeline = ToolPipeline(dispatcher)
    pipeline.add_logging()
    pipeline.add_timing()

    result = pipeline.dispatch("ping", "{}")
    assert result["output"] == "pong"
    ```
    """
    def __new__(cls, dispatcher: ToolDispatcher) -> ToolPipeline:
        r"""
        Create a pipeline wrapping the given ``ToolDispatcher``.

        The dispatcher's handlers are copied; changes after construction are
        **not** reflected.  Register handlers before building the pipeline.

        Args:
            dispatcher: An :class:`ToolDispatcher` with handlers already registered.
        """
    def add_logging(self, log_params: builtins.bool = False) -> None:
        r"""
        Add a :class:`LoggingMiddleware` to the pipeline.

        Args:
            log_params: If ``True``, log action parameters (default: ``False``).
        """
    def add_timing(self) -> TimingMiddleware:
        r"""
        Add a :class:`TimingMiddleware` to the pipeline.

        Returns the middleware instance so the caller can query timings.
        """
    def add_audit(self, record_params: builtins.bool = True) -> AuditMiddleware:
        r"""
        Add an :class:`AuditMiddleware` to the pipeline.

        Returns the middleware instance so the caller can query records.

        Args:
            record_params: If ``True``, include parameters in audit records (default: ``True``).
        """
    def add_rate_limit(self, max_calls: builtins.int, window_ms: builtins.int) -> RateLimitMiddleware:
        r"""
        Add a :class:`RateLimitMiddleware` to the pipeline.

        Returns the middleware instance so the caller can query counters.

        Args:
            max_calls:  Maximum allowed calls per action per ``window_ms``.
            window_ms:  Window size in milliseconds.
        """
    def add_callable(self, before_fn: typing.Optional[typing.Any] = None, after_fn: typing.Optional[typing.Any] = None) -> None:
        r"""
        Add a custom Python callable middleware.

        Args:
            before_fn: Optional callable ``(action_name: str) -> None`` called before dispatch.
            after_fn:  Optional callable ``(action_name: str, success: bool) -> None`` called after.
        """
    def dispatch(self, action_name: builtins.str, params_json: builtins.str = 'null', meta_json: builtins.str = 'null') -> dict:
        r"""
        Dispatch an action through the middleware pipeline.

        Runs all middleware hooks, then calls the registered Python handler.

        Args:
            action_name: Name of the registered action.
            params_json: JSON-encoded parameters (default: ``"null"``).

        Returns:
            A dict with keys ``"action"`` (str), ``"output"`` (Any), ``"validation_skipped"`` (bool).

        Raises:
            KeyError:     No handler registered for ``action_name``.
            ValueError:   Invalid JSON or schema validation failure.
            RuntimeError: Handler error or rate-limit exceeded.
        """
    def register_handler(self, action_name: builtins.str, handler: typing.Any) -> None:
        r"""
        Register a Python callable as a handler for ``action_name``.

        This mirrors ``ToolDispatcher.register_handler`` but operates on the
        pipeline's internal dispatcher.
        """
    def middleware_count(self) -> builtins.int:
        r"""
        Number of middleware currently registered.
        """
    def middleware_names(self) -> builtins.list[builtins.str]:
        r"""
        Names of registered middleware in registration order.
        """
    def handler_count(self) -> builtins.int:
        r"""
        Number of handlers registered.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolRecorder:
    r"""
    Records per-Action execution time and success/failure counters.

    # Example

    ```python
    from dcc_mcp_core import ToolRecorder

    recorder = ToolRecorder("my-scope")

    guard = recorder.start("create_sphere", "maya")
    # ... do work ...
    guard.finish(success=True)

    metrics = recorder.metrics("create_sphere")
    print(metrics.invocation_count, metrics.success_rate())
    ```
    """
    def __new__(cls, scope: builtins.str) -> ToolRecorder:
        r"""
        Create a new `ToolRecorder` for the given scope name.
        """
    def start(self, action_name: builtins.str, dcc_name: builtins.str) -> RecordingGuard:
        r"""
        Start timing an action and return a guard object.
        """
    def metrics(self, action_name: builtins.str) -> typing.Optional[ToolMetrics]:
        r"""
        Get aggregated metrics for a specific action.

        Returns `None` if no data exists for this action.
        """
    def all_metrics(self) -> builtins.list[ToolMetrics]:
        r"""
        Get aggregated metrics for all recorded actions.
        """
    def reset(self) -> None:
        r"""
        Reset all in-memory statistics.
        """

@typing.final
class ToolRegistry:
    r"""
    Thread-safe Action registry.

    Each registry instance is independent, eliminating cross-DCC pollution.
    """
    def __new__(cls) -> ToolRegistry: ...
    def register_batch(self, actions: typing.Sequence[typing.Any]) -> None: ...
    def unregister(self, name: builtins.str, dcc_name: typing.Optional[builtins.str] = None) -> builtins.bool: ...
    def register(self, name: builtins.str, description: builtins.str = '', category: builtins.str = '', tags: typing.Sequence[builtins.str] = [], dcc: builtins.str = 'python', version: builtins.str = '1.0.0', input_schema: typing.Optional[builtins.str] = None, output_schema: typing.Optional[builtins.str] = None, source_file: typing.Optional[builtins.str] = None, skill_name: typing.Optional[builtins.str] = None, group: builtins.str = '', enabled: builtins.bool = True, required_capabilities: typing.Optional[typing.Sequence[builtins.str]] = None, execution: builtins.str = 'sync', timeout_hint_secs: typing.Optional[builtins.int] = None, job_strategy: builtins.str = 'monolithic', thread_affinity: builtins.str = 'any', enforce_thread_affinity: builtins.bool = False, search_aliases: typing.Optional[typing.Sequence[builtins.str]] = None) -> None: ...
    def set_group_enabled(self, group: builtins.str, enabled: builtins.bool) -> builtins.int: ...
    def set_action_enabled(self, name: builtins.str, enabled: builtins.bool) -> builtins.bool: ...
    def list_actions_enabled(self, dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[typing.Any]: ...
    def list_actions_in_group(self, group: builtins.str) -> builtins.list[typing.Any]: ...
    def list_groups(self) -> builtins.list[builtins.str]: ...
    def get_action(self, name: builtins.str, dcc_name: typing.Optional[builtins.str] = None) -> typing.Optional[typing.Any]: ...
    def list_actions_for_dcc(self, dcc_name: builtins.str) -> builtins.list[builtins.str]: ...
    def list_actions(self, dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[typing.Any]: ...
    def get_all_dccs(self) -> builtins.list[builtins.str]: ...
    def search_actions(self, category: typing.Optional[builtins.str] = None, tags: typing.Sequence[builtins.str] = [], dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[typing.Any]: ...
    def get_categories(self, dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[builtins.str]: ...
    def get_tags(self, dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[builtins.str]: ...
    def count_actions(self, category: typing.Optional[builtins.str] = None, tags: typing.Sequence[builtins.str] = [], dcc_name: typing.Optional[builtins.str] = None) -> builtins.int: ...
    def reset(self) -> None: ...
    def __len__(self) -> builtins.int: ...
    def __contains__(self, name: builtins.str) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...

@typing.final
class ToolResult:
    r"""
    Python-facing ToolResult.
    """
    @property
    def success(self) -> builtins.bool: ...
    @property
    def message(self) -> builtins.str: ...
    @message.setter
    def message(self, value: builtins.str) -> None: ...
    @property
    def prompt(self) -> typing.Optional[builtins.str]: ...
    @property
    def error(self) -> typing.Optional[builtins.str]: ...
    @property
    def context(self) -> dict: ...
    @property
    def _meta(self) -> dict: ...
    def __eq__(self, other: builtins.object, /) -> builtins.bool: ...
    def __new__(cls, success: builtins.bool = True, message: builtins.str = '', prompt: typing.Optional[builtins.str] = None, error: typing.Optional[builtins.str] = None, context: typing.Optional[dict] = None, *, _meta: typing.Optional[dict] = None) -> ToolResult: ...
    def with_error(self, error: builtins.str) -> ToolResult:
        r"""
        Create a new instance with error information.
        """
    def with_context(self, **kwargs: typing.Any) -> ToolResult:
        r"""
        Create a new instance with updated context.
        """
    def to_dict(self) -> dict:
        r"""
        Convert to dictionary.
        """
    def to_json(self) -> builtins.str:
        r"""
        Serialize to a JSON string.
        """
    def __iter__(self) -> typing.Any:
        r"""
        Iterate over key-value pairs (mapping protocol).
        """
    def keys(self) -> builtins.list[builtins.str]:
        r"""
        Return the list of field names (part of the mapping protocol).
        """
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class ToolValidator:
    r"""
    Validates JSON-encoded action parameters against a JSON Schema.

    Create with :meth:`from_schema_json` or :meth:`from_action_registry`.

    Example::

        import json
        from dcc_mcp_core import ToolRegistry, ToolValidator

        schema = json.dumps({
            "type": "object",
            "required": ["radius"],
            "properties": {"radius": {"type": "number", "minimum": 0.0}}
        })
        v = ToolValidator.from_schema_json(schema)
        ok, errors = v.validate('{"radius": 1.0}')
        assert ok
        ok, errors = v.validate("{}")
        assert not ok
    """
    @staticmethod
    def from_schema_json(schema_json: builtins.str) -> ToolValidator:
        r"""
        Create a validator from a JSON Schema string.

        Raises:
            ValueError: If ``schema_json`` is not valid JSON.
        """
    @staticmethod
    def from_action_registry(registry: ToolRegistry, action_name: builtins.str, dcc_name: typing.Optional[builtins.str] = None) -> ToolValidator:
        r"""
        Create a validator from an action in an :class:`ToolRegistry`.

        Raises:
            KeyError: If the action is not found in the registry.
        """
    def validate(self, params_json: builtins.str) -> tuple[builtins.bool, builtins.list[builtins.str]]:
        r"""
        Validate JSON-encoded parameters.

        Returns:
            ``(bool, list[str])`` — True + empty list on success; False + errors on failure.

        Raises:
            ValueError: If ``params_json`` is not valid JSON.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class TransportAddress:
    r"""
    Python-facing transport address for DCC communication.

    Represents a protocol-agnostic endpoint: TCP, Named Pipe, or Unix Socket.

    ```python,ignore
    from dcc_mcp_core import TransportAddress

    # TCP address
    addr = TransportAddress.tcp("127.0.0.1", 18812)

    # Named Pipe (Windows)
    addr = TransportAddress.named_pipe("dcc-mcp-maya-12345")

    # Unix Socket (macOS/Linux)
    addr = TransportAddress.unix_socket("/tmp/dcc-mcp-maya.sock")

    # Auto-detect best local transport
    addr = TransportAddress.default_local("maya", 12345)

    # Parse from URI string
    addr = TransportAddress.parse("tcp://127.0.0.1:9000")

    print(addr.scheme)      # "tcp", "pipe", or "unix"
    print(addr.is_local)    # True/False
    print(str(addr))        # "tcp://127.0.0.1:18812"
    ```
    """
    @property
    def scheme(self) -> builtins.str:
        r"""
        Get the transport scheme name ("tcp", "pipe", or "unix").
        """
    @property
    def is_local(self) -> builtins.bool:
        r"""
        Check if this is a local (same-machine) transport.
        """
    @property
    def is_tcp(self) -> builtins.bool:
        r"""
        Check if this is a TCP address.
        """
    @property
    def is_named_pipe(self) -> builtins.bool:
        r"""
        Check if this is a Named Pipe address.
        """
    @property
    def is_unix_socket(self) -> builtins.bool:
        r"""
        Check if this is a Unix Domain Socket address.
        """
    @staticmethod
    def tcp(host: builtins.str, port: builtins.int) -> TransportAddress:
        r"""
        Create a TCP transport address.
        """
    @staticmethod
    def named_pipe(name: builtins.str) -> TransportAddress:
        r"""
        Create a Named Pipe transport address (Windows).

        Automatically prepends `\\.\pipe\` if not already present.
        """
    @staticmethod
    def unix_socket(path: builtins.str) -> TransportAddress:
        r"""
        Create a Unix Domain Socket transport address (macOS/Linux).
        """
    @staticmethod
    def default_local(dcc_type: builtins.str, pid: builtins.int) -> TransportAddress:
        r"""
        Generate the optimal local transport for the current platform.

        Windows → Named Pipe, macOS/Linux → Unix Socket.
        """
    @staticmethod
    def default_pipe_name(dcc_type: builtins.str, pid: builtins.int) -> TransportAddress:
        r"""
        Generate a default Named Pipe name for a DCC instance.
        """
    @staticmethod
    def default_unix_socket(dcc_type: builtins.str, pid: builtins.int) -> TransportAddress:
        r"""
        Generate a default Unix Socket path for a DCC instance.
        """
    @staticmethod
    def parse(s: builtins.str) -> TransportAddress:
        r"""
        Parse a transport address from a URI string.

        Supports ``tcp://host:port``, ``pipe://name``, and ``unix://path`` schemes.

        # Examples (Python)

        ```python,ignore
        addr = TransportAddress.parse("tcp://127.0.0.1:9000")
        assert addr.is_tcp

        addr2 = TransportAddress.parse("pipe://dcc-mcp-maya")
        assert addr2.is_named_pipe
        ```
        """
    def to_connection_string(self) -> builtins.str:
        r"""
        Get the connection string (e.g. "tcp://127.0.0.1:18812").
        """
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...
    def __eq__(self, other: TransportAddress) -> builtins.bool: ...
    def __hash__(self) -> builtins.int: ...

@typing.final
class UsdPrim:
    r"""
    A prim (primitive) within a USD stage.
    """
    @property
    def path(self) -> SdfPath:
        r"""
        Absolute path of this prim.
        """
    @property
    def type_name(self) -> builtins.str:
        r"""
        USD type name (e.g. ``"Mesh"``, ``"Camera"``).
        """
    @property
    def active(self) -> builtins.bool:
        r"""
        Whether this prim is active.
        """
    @property
    def name(self) -> builtins.str:
        r"""
        Last path element name.
        """
    def __repr__(self) -> builtins.str: ...
    def set_attribute(self, name: builtins.str, value: VtValue) -> None:
        r"""
        Set or update an attribute value.
        """
    def get_attribute(self, name: builtins.str) -> typing.Optional[VtValue]:
        r"""
        Get an attribute value.  Returns ``None`` if not found.
        """
    def attribute_names(self) -> builtins.list[builtins.str]:
        r"""
        List all attribute names.
        """
    def attributes_summary(self) -> dict:
        r"""
        Return a dict of ``{attr_name: type_name}``.
        """
    def has_api(self, schema: builtins.str) -> builtins.bool:
        r"""
        Whether the given API schema is applied to this prim.
        """

@typing.final
class UsdStage:
    r"""
    A composed USD stage — the primary unit of scene exchange.

    Example::

        from dcc_mcp_core import UsdStage, SdfPath, VtValue

        stage = UsdStage("my_scene")
        prim = stage.define_prim("/World/Cube", "Mesh")
        stage.set_attribute("/World/Cube", "radius", VtValue.from_float(1.0))
        print(stage.export_usda())
    """
    @property
    def name(self) -> builtins.str:
        r"""
        Stage name.
        """
    @property
    def id(self) -> builtins.str:
        r"""
        Unique stage ID (UUID string).
        """
    @property
    def default_prim(self) -> typing.Optional[builtins.str]:
        r"""
        Default prim path hint.
        """
    @property
    def up_axis(self) -> builtins.str:
        r"""
        Up axis of the root layer (``"Y"`` or ``"Z"``).
        """
    @up_axis.setter
    def up_axis(self, value: builtins.str) -> None: ...
    @property
    def meters_per_unit(self) -> builtins.float:
        r"""
        Meters per unit of the root layer.
        """
    @property
    def fps(self) -> typing.Optional[builtins.float]:
        r"""
        Frames per second.
        """
    @fps.setter
    def fps(self, value: typing.Optional[builtins.float]) -> None: ...
    @property
    def start_time_code(self) -> typing.Optional[builtins.float]:
        r"""
        Start time code.
        """
    @start_time_code.setter
    def start_time_code(self, value: typing.Optional[builtins.float]) -> None: ...
    @property
    def end_time_code(self) -> typing.Optional[builtins.float]:
        r"""
        End time code.
        """
    @end_time_code.setter
    def end_time_code(self, value: typing.Optional[builtins.float]) -> None: ...
    @default_prim_prop.setter
    def default_prim_prop(self, value: typing.Optional[builtins.str]) -> None: ...
    @meters_per_unit_prop.setter
    def meters_per_unit_prop(self, value: builtins.float) -> None: ...
    def __new__(cls, name: builtins.str) -> UsdStage:
        r"""
        Create a new empty stage with the given name.
        """
    def __repr__(self) -> builtins.str: ...
    def set_default_prim(self, path: builtins.str) -> None:
        r"""
        Set the default prim path (callable method form).
        """
    def define_prim(self, path: builtins.str, type_name: builtins.str) -> UsdPrim:
        r"""
        Define a prim at the given path with the given USD type name.

        Returns the newly created (or replaced) prim.
        """
    def get_prim(self, path: builtins.str) -> typing.Optional[UsdPrim]:
        r"""
        Get a prim by path.  Returns ``None`` if not found.
        """
    def has_prim(self, path: builtins.str) -> builtins.bool:
        r"""
        Whether the stage has a prim at the given path.
        """
    def remove_prim(self, path: builtins.str) -> builtins.bool:
        r"""
        Remove a prim from the root layer.  Returns ``True`` if removed.
        """
    def prim_count(self) -> builtins.int:
        r"""
        Return the number of prims in the root layer.
        """
    def list_prims(self) -> builtins.list[UsdPrim]:
        r"""
        Return all prims as a list of ``UsdPrim`` objects (alias for `traverse`).
        """
    def traverse(self) -> builtins.list[UsdPrim]:
        r"""
        Return all prims as a list of ``UsdPrim`` objects.
        """
    def prims_of_type(self, type_name: builtins.str) -> builtins.list[UsdPrim]:
        r"""
        Return all prims of the given USD type.
        """
    def set_attribute(self, prim_path: builtins.str, attr_name: builtins.str, value: VtValue) -> None:
        r"""
        Set an attribute on a prim.
        """
    def get_attribute(self, prim_path: builtins.str, attr_name: builtins.str) -> typing.Optional[VtValue]:
        r"""
        Get an attribute value from a prim.  Returns ``None`` if prim or attr not found.
        """
    def metrics(self) -> dict:
        r"""
        Return stage metrics as a dictionary.
        """
    def to_json(self) -> builtins.str:
        r"""
        Serialize the stage to a JSON string.
        """
    @staticmethod
    def from_json(json: builtins.str) -> UsdStage:
        r"""
        Deserialize a stage from a JSON string.
        """
    def export_usda(self) -> builtins.str:
        r"""
        Export the stage as USDA (USD ASCII) text.
        """
    def set_meters_per_unit(self, mpu: builtins.float) -> None:
        r"""
        Set the meters per unit value (callable method form).
        """

@typing.final
class VersionConstraint:
    @staticmethod
    def parse(s: builtins.str) -> VersionConstraint: ...
    def matches(self, version: SemVer) -> builtins.bool: ...
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class VersionedRegistry:
    r"""
    Multi-version action registry.

    Allows multiple versions of the same `(action_name, dcc_name)` pair to coexist.
    Older versions are kept until explicitly removed, enabling backward-compatible
    resolution through the [`CompatibilityRouter`].
    """
    def __new__(cls) -> VersionedRegistry: ...
    def register_versioned(self, name: builtins.str, dcc: builtins.str, version: builtins.str, description: builtins.str = '', category: builtins.str = '', tags: typing.Optional[typing.Sequence[builtins.str]] = None) -> None:
        r"""
        Register an action version.
        """
    def remove(self, name: builtins.str, dcc: builtins.str, constraint: builtins.str) -> builtins.int:
        r"""
        Remove all versions of `(name, dcc)` that satisfy the constraint string.
        """
    def versions(self, name: builtins.str, dcc: builtins.str) -> builtins.list[builtins.str]:
        r"""
        Return all registered versions for `(name, dcc)`, sorted ascending.
        """
    def latest_version(self, name: builtins.str, dcc: builtins.str) -> typing.Optional[builtins.str]:
        r"""
        Return the highest registered version string, or `None` if not registered.
        """
    def keys(self) -> builtins.list[tuple[builtins.str, builtins.str]]:
        r"""
        Return all registered `(name, dcc)` keys.
        """
    def total_entries(self) -> builtins.int:
        r"""
        Return the total number of registered versioned entries.
        """
    def resolve(self, name: builtins.str, dcc: builtins.str, constraint: builtins.str) -> typing.Optional[dict]:
        r"""
        Resolve the best-matching version given a constraint string.
        """
    def resolve_all(self, name: builtins.str, dcc: builtins.str, constraint: builtins.str) -> builtins.list[dict]:
        r"""
        Return all action metadata dicts that satisfy `constraint`, sorted ascending.
        """
    def __repr__(self) -> builtins.str: ...

@typing.final
class VtValue:
    r"""
    A USD variant value (bool, int, float, string, vec3f, etc.).
    """
    @property
    def type_name(self) -> builtins.str:
        r"""
        The USD type name string (e.g. ``"float3"``, ``"token"``).
        """
    def __repr__(self) -> builtins.str: ...
    @staticmethod
    def from_bool(v: builtins.bool) -> VtValue:
        r"""
        Create a bool value.
        """
    @staticmethod
    def from_int(v: builtins.int) -> VtValue:
        r"""
        Create an int value.
        """
    @staticmethod
    def from_float(v: builtins.float) -> VtValue:
        r"""
        Create a float value.
        """
    @staticmethod
    def from_string(v: builtins.str) -> VtValue:
        r"""
        Create a string value.
        """
    @staticmethod
    def from_token(v: builtins.str) -> VtValue:
        r"""
        Create a token value (USD enum identifier).
        """
    @staticmethod
    def from_asset(v: builtins.str) -> VtValue:
        r"""
        Create an asset path value.
        """
    @staticmethod
    def from_vec3f(x: builtins.float, y: builtins.float, z: builtins.float) -> VtValue:
        r"""
        Create a 3D float vector.
        """
    def to_python(self) -> typing.Any:
        r"""
        Convert to a Python primitive.  Returns ``None`` for array/matrix types.
        """

@typing.final
class WindowFinder:
    r"""
    Python wrapper for [`WindowFinder`].
    """
    def __new__(cls) -> WindowFinder: ...
    def find(self, target: CaptureTarget) -> typing.Optional[WindowInfo]:
        r"""
        Resolve a :class:`CaptureTarget` to a window.

        Returns ``None`` instead of raising when no matching window exists.
        """
    def enumerate(self) -> builtins.list[WindowInfo]:
        r"""
        Return a list of all visible top-level windows.
        """

@typing.final
class WindowInfo:
    r"""
    Python wrapper for [`crate::window::WindowInfo`].
    """
    @property
    def handle(self) -> builtins.int: ...
    @property
    def pid(self) -> builtins.int: ...
    @property
    def title(self) -> builtins.str: ...
    @property
    def rect(self) -> tuple[builtins.int, builtins.int, builtins.int, builtins.int]: ...

@typing.final
class DccErrorCode(enum.Enum):
    r"""
    Python-facing enum for DCC error codes.
    """
    CONNECTION_FAILED = ...
    TIMEOUT = ...
    SCRIPT_ERROR = ...
    NOT_RESPONDING = ...
    UNSUPPORTED = ...
    PERMISSION_DENIED = ...
    INVALID_INPUT = ...
    SCENE_ERROR = ...
    INTERNAL = ...

    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class DispatchStatus(enum.Enum):
    r"""
    Python-facing enum for application-level dispatch readiness.

    ADR 018 — replaces the free-form ``dispatch_status`` metadata string.

    ```python,ignore
    from dcc_mcp_core import DispatchStatus

    status = DispatchStatus.READY
    print(status)  # "READY"
    ```
    """
    READY = ...
    r"""
    Instance has reported dispatch-ready.
    """
    PENDING = ...
    r"""
    Instance is alive but not yet dispatch-ready.
    """
    FAILED = ...
    r"""
    Instance reported a dispatch failure.
    """
    UNKNOWN = ...
    r"""
    Instance has not reported dispatch status.
    """

    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class PySceneDataKind(enum.Enum):
    r"""
    Kind of DCC scene data stored in a shared scene buffer.
    """
    Geometry = ...
    AnimationCache = ...
    Screenshot = ...
    Arbitrary = ...

@typing.final
class ScriptLanguage(enum.Enum):
    r"""
    Python-facing enum for DCC script languages.

    ```python
    from dcc_mcp_core import ScriptLanguage

    lang = ScriptLanguage.PYTHON
    print(lang)  # "python"
    ```
    """
    PYTHON = ...
    MEL = ...
    MAXSCRIPT = ...
    HSCRIPT = ...
    VEX = ...
    LUA = ...
    CSHARP = ...
    BLUEPRINT = ...
    JAVASCRIPT = ...
    TYPESCRIPT = ...

    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class SerializeFormat(enum.Enum):
    r"""
    Supported serialization formats for `ToolResult`.

    The default is [`SerializeFormat::Json`] (UTF-8 text, human-readable).
    [`SerializeFormat::MsgPack`] produces compact binary (MessagePack via `rmp-serde`)
    and is suitable for high-throughput or binary transport scenarios.

    # Future extensibility
    Additional formats (e.g. CBOR, Bincode) can be added as new variants without
    breaking the existing API.
    """
    Json = ...
    r"""
    JSON (default): UTF-8 text, human-readable, widely compatible.
    """
    MsgPack = ...
    r"""
    MessagePack: compact binary encoding via `rmp-serde`.
    """

    def __repr__(self) -> builtins.str: ...

@typing.final
class ServiceStatus(enum.Enum):
    r"""
    Python-facing enum for DCC service status.

    ```python,ignore
    from dcc_mcp_core import ServiceStatus

    status = ServiceStatus.AVAILABLE
    print(status)  # "AVAILABLE"
    ```
    """
    AVAILABLE = ...
    r"""
    Service is available and accepting connections.
    """
    BUSY = ...
    r"""
    Service is busy (processing a request).
    """
    UNREACHABLE = ...
    r"""
    Service is unreachable (health check failed).
    """
    SHUTTING_DOWN = ...
    r"""
    Service is shutting down.
    """
    BOOTING = ...
    r"""
    Service process is alive but its embedded DCC host is still
    initialising (readiness probe returns red).
    Introduced in dcc-mcp-core#713.
    """
    STALE = ...
    r"""
    Service has already been marked stale by a probe or registry owner.
    """

    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class SkillRuntimeKind(enum.Enum):
    r"""
    Declarative optional runtime dependency category for adapter skills.
    """
    PythonPackage = ...
    r"""
    Python package or wheel dependency, commonly checked by module name.
    """
    PythonExtra = ...
    r"""
    Python optional extra such as `dcc-mcp-openusd[usd-core]`.
    """
    Binary = ...
    r"""
    Command-line binary expected on `PATH`.
    """
    EnvVar = ...
    r"""
    Environment variable expected to be non-empty.
    """
    Feature = ...
    r"""
    Declarative feature level with no automatic probe.
    """

@typing.final
class SkillRuntimeState(enum.Enum):
    r"""
    Resolved optional runtime state surfaced through discovery APIs.
    """
    Available = ...
    r"""
    All declared runtime checks are satisfied.
    """
    Degraded = ...
    r"""
    Optional runtime is absent; skill can still run with reduced capability.
    """
    Missing = ...
    r"""
    Required runtime is absent; the advertised capability is not callable.
    """

@typing.final
class SkillScope(enum.Enum):
    r"""
    Trust level / origin scope of a skill.

    Determines default policy and precedence when multiple skills with
    the same name exist at different scope levels.

    Precedence (highest → lowest): `Admin > System > Team > User > Repo`

    # Example SKILL.md usage
    Scope is **not** declared in the SKILL.md file itself — it is inferred
    at discovery time from the directory the skill was found in:

    | Path pattern                            | Scope  |
    |----------------------------------------|--------|
    | `<project>/.dcc_skills/`               | Repo   |
    | `~/.dcc_mcp/skills/`                   | User   |
    | `~/.dcc_mcp/skills/team/`              | Team   |
    | `<install>/share/dcc_mcp/skills/`      | System |
    | Managed enterprise distribution        | Admin  |
    """
    Repo = ...
    r"""
    Project-local skill (e.g. `./<project>/.dcc_skills/`).

    Lowest trust — cannot silently override higher-scope skills.
    """
    User = ...
    r"""
    User-level skill (e.g. `~/.dcc_mcp/skills/`).
    """
    Team = ...
    r"""
    Team-level skill shared within a studio or team.
    """
    System = ...
    r"""
    System-level skill bundled with the package (read-only).
    """
    Admin = ...
    r"""
    Enterprise/admin-managed skill.  Highest trust.
    """

    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

@typing.final
class TransportScheme(enum.Enum):
    r"""
    Python-facing transport selection strategy.

    ```python,ignore
    from dcc_mcp_core import TransportScheme

    scheme = TransportScheme.AUTO          # Auto-detect best transport
    scheme = TransportScheme.TCP_ONLY      # Always use TCP
    scheme = TransportScheme.PREFER_IPC    # Prefer IPC, fallback to TCP
    ```
    """
    AUTO = ...
    r"""
    Automatically select the best transport based on locality.
    """
    TCP_ONLY = ...
    r"""
    Always use TCP.
    """
    PREFER_NAMED_PIPE = ...
    r"""
    Prefer Named Pipe on Windows, fall back to TCP.
    """
    PREFER_UNIX_SOCKET = ...
    r"""
    Prefer Unix Domain Socket, fall back to TCP.
    """
    PREFER_IPC = ...
    r"""
    Prefer any IPC, fall back to TCP.
    """

    def select_address(self, dcc_type: builtins.str, host: builtins.str, port: builtins.int, pid: typing.Optional[builtins.int] = None) -> TransportAddress:
        r"""
        Select the optimal transport address for a connection.

        Args:
            dcc_type: DCC application type (e.g. "maya").
            host: Target host address.
            port: Target port.
            pid: Optional DCC process PID (needed for IPC naming).

        Returns:
            TransportAddress — the selected transport endpoint.
        """
    def __repr__(self) -> builtins.str: ...
    def __str__(self) -> builtins.str: ...

def artefact_get_bytes(uri: builtins.str) -> typing.Any:
    r"""
    Read back the raw bytes for an ``artefact://`` URI. Raises ``IOError``
    when the URI is unknown.
    """

def artefact_list() -> builtins.list[FileRef]:
    r"""
    List every known artefact, returning a list of :class:`FileRef`.
    """

def artefact_put_bytes(data: builtins.bytes, mime: typing.Optional[builtins.str] = None, display_name: typing.Optional[builtins.str] = None, producer_job_id: typing.Optional[builtins.str] = None, tool_call_id: typing.Optional[builtins.str] = None, session_id: typing.Optional[builtins.str] = None, correlation_id: typing.Optional[builtins.str] = None, ttl_secs: typing.Optional[builtins.int] = None) -> FileRef:
    r"""
    Store raw ``bytes`` and return a :class:`FileRef`.
    """

def artefact_put_file(path: builtins.str, mime: typing.Optional[builtins.str] = None, display_name: typing.Optional[builtins.str] = None, producer_job_id: typing.Optional[builtins.str] = None, tool_call_id: typing.Optional[builtins.str] = None, session_id: typing.Optional[builtins.str] = None, correlation_id: typing.Optional[builtins.str] = None, ttl_secs: typing.Optional[builtins.int] = None) -> FileRef:
    r"""
    Store the file at ``path`` and return a :class:`FileRef`.

    The default store is a content-addressed filesystem store under
    ``<temp_dir>/dcc-mcp-artefacts``. ``McpHttpServer`` installs its own
    store on start, so inside a server process this helper routes to the
    server-owned store automatically.
    """

def atomic_write_bytes(path: builtins.str, data: builtins.bytes, create_parents: builtins.bool = True, max_bytes: typing.Optional[builtins.int] = None) -> builtins.str:
    r"""
    Atomically write ``data`` to ``path`` and return the path.
    """

def atomic_write_text(path: builtins.str, text: builtins.str, create_parents: builtins.bool = True, max_bytes: typing.Optional[builtins.int] = None) -> builtins.str:
    r"""
    Atomically write UTF-8 ``text`` to ``path`` and return the path.
    """

def bytes_digest_sha256(data: builtins.bytes, max_bytes: typing.Optional[builtins.int] = None) -> builtins.str:
    r"""
    Hash ``data`` with SHA-256 and return the lowercase hex digest.
    """

def correct_python_executable(path: builtins.str) -> pathlib.Path:
    r"""
    If `path` is a known DCC GUI binary with a headless-Python sibling on
    disk, return that sibling path; otherwise return `path` unchanged.
    Convenience for one-shot fixing of `DCC_MCP_PYTHON_EXECUTABLE`.
    """

def deserialize_result(data: typing.Any, format: SerializeFormat = SerializeFormat.Json) -> ToolResult:
    r"""
    Deserialize a `str` (JSON) or `bytes` (MsgPack) into a `ToolResult`.
    """

def ensure_within_root(root: builtins.str, path: builtins.str, must_exist: builtins.bool = False) -> builtins.str:
    r"""
    Resolve ``path`` under ``root`` and reject paths that escape the root.
    """

def error_result(message: builtins.str, error: builtins.str, prompt: typing.Optional[builtins.str] = None, possible_solutions: typing.Optional[typing.Sequence[builtins.str]] = None, *, _meta: typing.Optional[dict] = None, **context: typing.Any) -> ToolResult: ...

def file_digest_sha256(path: builtins.str, max_bytes: typing.Optional[builtins.int] = None) -> builtins.str:
    r"""
    Stream-hash a file with SHA-256 and return the lowercase hex digest.
    """

def from_exception(error_message: builtins.str, message: typing.Optional[builtins.str] = None, prompt: typing.Optional[builtins.str] = None, include_traceback: builtins.bool = True, possible_solutions: typing.Optional[typing.Sequence[builtins.str]] = None, *, _meta: typing.Optional[dict] = None, **context: typing.Any) -> ToolResult: ...

def gc_orphans(max_age_secs: builtins.float) -> builtins.int:
    r"""
    Scan for and remove stale ``dcc_shm_*`` shared memory segments.

    On Linux this scans ``/dev/shm``; on macOS it scans ``/tmp``;
    on Windows it is a no-op (the OS reclaims named file-mappings on
    last close).

    Returns the number of segments removed.

    Parameters
    ----------
    max_age_secs : float
        Minimum age (in seconds) for a segment to be considered stale.
        Segments whose TTL has expired **or** whose creation time is older
        than ``max_age_secs`` are removed.
    """

def init_default_telemetry() -> None:
    r"""
    Initialise a minimal no-op telemetry provider if one has not been set yet.

    Silences the ``NoopMeterProvider`` warning that OpenTelemetry emits when
    ``global::meter()`` is called before any provider has been registered
    (issue #467).  Safe to call multiple times — a no-op when already
    initialised.

    Example::

        from dcc_mcp_core import init_default_telemetry

        # At server startup, before any MCP tools run:
        init_default_telemetry()

    Raises:
        RuntimeError: If provider initialisation fails for a reason other than
            "already initialized".
    """

def is_gui_executable(path: builtins.str) -> typing.Optional[GuiExecutableHint]:
    r"""
    Detect a known DCC GUI binary. Returns `None` for Python interpreters
    (`python.exe`, `mayapy`, `hython` …) and for unknown vendor binaries.
    """

def is_telemetry_initialized() -> builtins.bool:
    r"""
    Return `True` if the global telemetry provider has been initialised.
    """

def lz4_compress(data: builtins.bytes, max_bytes: typing.Optional[builtins.int] = None) -> typing.Any:
    r"""
    Compress ``data`` using LZ4 frame encoding.
    """

def lz4_decompress(data: builtins.bytes, max_bytes: typing.Optional[builtins.int] = None) -> typing.Any:
    r"""
    Decompress LZ4 frame-encoded ``data`` with an output size guard.
    """

def mpu_to_units(mpu: builtins.float) -> builtins.str:
    r"""
    Convert meters per unit to a unit string.
    """

def normalize_tool_arguments(arguments: typing.Optional[typing.Any] = None) -> typing.Any:
    r"""
    Normalize Python ``arguments`` for MCP ``tools/call`` and REST ``/v1/call``.
    """

def normalize_tool_meta(meta: typing.Optional[typing.Any] = None) -> typing.Any:
    r"""
    Normalize Python ``_meta`` to an object or ``None``.
    """

def parse_skill_md(skill_dir: builtins.str) -> typing.Optional[SkillMetadata]:
    r"""
    Python wrapper for [`parse_skill_md`].

    Accepts either a skill directory path or a direct path to a `SKILL.md` file.
    If a file path is given, the parent directory is used automatically.

    Returns `None` if the directory contains no valid `SKILL.md`.
    Raises `FileNotFoundError` if the path does not exist at all.
    """

def scan_and_load(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_and_load_lenient(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_and_load_strict(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]:
    r"""
    Strict pipeline (issue maya#138) — same as [`py_scan_and_load`] but
    raises `ValueError` when any directory was silently skipped, so
    embedders can fail start-up loudly instead of discovering missing
    tools at run-time.
    """

def scan_and_load_team(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_and_load_team_lenient(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_and_load_user(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_and_load_user_lenient(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> tuple[builtins.list[SkillMetadata], builtins.list[builtins.str]]: ...

def scan_skill_paths(extra_paths: typing.Optional[typing.Sequence[builtins.str]] = None, dcc_name: typing.Optional[builtins.str] = None) -> builtins.list[builtins.str]:
    r"""
    Convenience function: scan with a fresh scanner.
    """

def scene_info_json_to_stage(scene_info_json: builtins.str, dcc_type: builtins.str = 'generic') -> UsdStage:
    r"""
    Convert a DCC ``SceneInfo`` dict to a ``UsdStage``.

    Args:
        scene_info_json: JSON string of a ``SceneInfo`` object.
        dcc_type: The DCC type string (e.g. ``"maya"``). Defaults to ``"generic"``.

    Returns:
        A ``UsdStage`` containing the converted scene.
    """

def serialize_result(result: ToolResult, format: SerializeFormat = SerializeFormat.Json) -> typing.Any:
    r"""
    Serialize a `ToolResult` to a string (JSON) or bytes (MsgPack).
    """

def shutdown_telemetry() -> None:
    r"""
    Shut down the global telemetry provider, flushing all pending data.
    """

def stage_to_scene_info_json(stage: UsdStage) -> builtins.str:
    r"""
    Convert a ``UsdStage`` to a JSON string representing ``SceneInfo``.
    """

def success_result(message: builtins.str, prompt: typing.Optional[builtins.str] = None, *, _meta: typing.Optional[dict] = None, **context: typing.Any) -> ToolResult: ...

def units_to_mpu(units: builtins.str) -> builtins.float:
    r"""
    Convert a unit string to meters per unit.
    """

def validate_action_id(name: builtins.str) -> None:
    r"""
    Validate an internal action id.

    Raises ``ValueError`` on any violation; returns ``None`` on success.
    """

def validate_action_result(result: typing.Any) -> ToolResult: ...

def validate_tool_name(name: builtins.str) -> None:
    r"""
    Validate an MCP wire-visible tool name.

    Raises ``ValueError`` on any violation; returns ``None`` on success.
    """
